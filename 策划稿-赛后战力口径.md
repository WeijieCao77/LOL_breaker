# 策划稿 · 赛后战力口径统一到「开赛那一刻」

**状态**：等作者审批。代码里暂未改（`main.ts` 的 `endMatch` 有注释指到这份文件）。
**起因**：玩家实锤「赛前自己和队伍体能都是满的，一打完结算就拿体能扣我的综合分，试了两次都一样」。
**已经做了的一半**：v20260909a 把**赛后拆解那张卡**改成读开赛体能。这份稿子说的是**另一半**。

---

## 一、现在是怎么错的

`endMatch()` 的执行顺序是：

1. `addFat(13)`（国际赛 20）——先把这场比赛的体能消耗记上
2. 然后才算 `myPw = power(myRoster(), S.fatigue, ...)`

而这一场的胜负，是在比赛过程中用**赛前体能**判出来的（`main.ts` 里每一局的赢面都读当时的 `S.fatigue`，一个系列赛打完之前没人扣过体能）。

所以 `myPw` 描述的是一支「已经打完、已经累了」的队伍，却被拿去解释一场用「还没累」的状态打出来的比赛。**它系统性地低估了你。**

这不是显示问题——`myPw` 下游有六个消费者。

## 二、谁在吃这个数

| 消费者 | 判据 | 口径改对之后 |
|---|---|---|
| 轮换降级 `demoteCheck` | `gap < −8` 免责一场 | 免责更少，连败更容易攒够 → **更容易被换下** |
| 首发试用 `rotationAfterMatch` | `gap < −6` 多给一场 | 宽限更少 → **更容易丢首发** |
| 更衣室 | 输球且 `gap > 4` → 全员信任 −3 | 触发更多 → **信任更低** |
| 逆风翻盘计数 | 赢球且 `gap < −2` | 触发更少 → **三次翻盘的心态突破更难拿** |
| 冷门播报 | 赢球且 `opPw − myPw > 4` → 热搜第一 | 触发更少 → **少一条大热度** |
| 个人数据合成 | `perf += gap × 0.022` | 数据更好看 → **表现分、教练信任略涨** |

六条里五条是变难，一条是变好。

## 三、实测的平衡位移

只把这一处改掉（天梯重置关闭），120 局同种子批测：

| 指标 | 现在 | 改成开赛体能 |
|---|---|---|
| 联赛夺冠 | 82.5% | 77.5% |
| 任一冠军 | 83.3% | 82.5% |
| MSI | 10.8% | 21.7% |
| 世界赛 | 25.8% | 30.0% |
| 场均冠军 | 2.37 | 2.49 |

MSI 翻倍是最扎眼的一条。方向上混着——联赛掉、国际赛涨——说明它不是一个单纯的削弱，而是把好几条判据的触发率同时挪了位。**这个量级不能不管就上。**

## 四、三个方案

### 方案 A · 平移阈值（推荐）

口径改对，同时把吃 `gap` 的阈值整体平移一个 δ，让**触发率不变**。

- δ = 改口径之后 `myPw` 的平均抬升量。先跑一局生涯，同时记两种算法的 `myPw`，取差值的中位数。
- 六条判据的阈值全部 `+δ`：`−8 → −8+δ`、`−6 → −6+δ`、`4 → 4+δ`、`−2 → −2+δ`。
- 个人数据那条系数 `0.022` 不动，但基准要减掉 δ（`(gap − δ) × 0.022`）。

**好处**：这是一次纯粹的口径修正，语义对了，数值没动。符合「结构定死、平衡只动数值」。
**验收**：240 局批测对比 `main`，五个主指标都要落在 1σ 以内。

### 方案 B · 直接改，接受位移

口径改对，阈值不动，把 MSI 翻倍和联赛下滑当成「本来就该是这样」。

**理由**：现在的低估让玩家在国际赛被系统性地当成弱队，改对之后国际赛成绩上来是**修复**而不是通胀。
**风险**：MSI 从 10.8% 到 21.7% 太大，不像单纯的修复。上线前至少要 240 局复核一次，确认不是噪声。

### 方案 C · 维持现状

只有拆解那张卡读开赛体能，其余六条继续吃赛后体能。

**理由**：玩家实锤的就是那张卡，已经解决。
**代价**：轮换、更衣室、冷门这几条判据继续用一个错的数，将来还会被玩家抓到（「我明明比对面强，怎么算我冷门输球」）。

## 五、推荐

**方案 A**。理由是这件事本质上是个 bug，不是一次平衡调整——玩家不该因为我们修了一个口径而突然更容易拿 MSI，也不该突然更容易被换下。先把 δ 量出来，阈值跟着平移，让批测回到 1σ 以内；等它稳了，再单独决定要不要动那几条判据的松紧。

## 六、落地清单（批准后执行）

1. `endMatch` 里的 `myPw` 改读 `m.fat0`（开赛快照，v20260909a 已经存了）
2. 写一段一次性量 δ 的脚本，取 240 局的中位数
3. `demoteCheck`、`rotationAfterMatch`、更衣室、逆风翻盘、冷门播报五处阈值 `+δ`，`synthSeriesStats` 的 `gap` 减 δ
4. 自检加一条：同一场比赛，赛后 `myPw` 必须等于用开赛体能算出来的数
5. 240 局批测对比 `main`，五个主指标 1σ 以内才算过
6. CHANGELOG 记一条，写清「这是口径修正，触发率没动」

---

*2026-09-09 · 破晓*
