# 策划稿 · 更衣室关系（队友两两）没有杠杆

**状态**：等作者审批。代码零改动。
**起因**：玩家「電瓶」在群里（2026-09-09）——

> 更衣室关系是个什么数值，怎么好像只有随机事件影响
> 跟我的都 100 了，他们之间怎么还是一坨

配图：他的存档，**平均信任 100**，而更衣室关系六对是 **49 / 52 / 52 / 56 / 58 / 59**。

**结论：他说对了，而且比他说的还严重——连随机事件都不影响它。**

---

## 一、`addRel()` 有零个调用点

`clout.ts:89` 定义并导出了 `addRel(a,b,n)`——**全仓库没有任何地方调用它**。

```
$ grep -rn "addRel(" demo/src/*.ts
demo/src/clout.ts:89:export function addRel(a,b,n){     ← 只有定义
```

也就是说：**队友两两之间的关系，没有任何一个「针对某一对」的改动入口。**
没有行动、没有随机事件、没有更衣室剧情能动它。

## 二、唯一的推手：每赛段一次的固定漂移

能动 `S.rel` 的只有 `relDrift(won)`（`clout.ts:116`）：

```ts
S.rel[k] = clamp(S.rel[k] + (won?1.6:-2.2) + (rnd()*5-2.5), 0, 100);
```

三个调用点：

| 出处 | 时机 | 效果 |
|---|---|---|
| `cloutTick()` `clout.ts:324` | **每赛段结算一次** | 胜率 ≥50% → **+1.6**，否则 −2.2（外加均值为 0 的 ±2.5 噪声） |
| `clout.ts:164` 挂牌队友 | 一次性 | `relDrift(false)` |
| `clout.ts:172` 买断 / 裁人 | 一次性 | `relDrift(false)` |

初始值 `initRelations()` 是 **42 + rnd()×20**（42–61），换人进来的新队友 `syncRelations()` 给 **38–51**。

一整个生涯 8 个赛季 = 16 个赛段，**全部打赢也只有 +25.6**，而中途每换一次人就有新的一对从 38–51 起步。
所以这个数会**永远趴在 50 附近**——这正是玩家截图里的 49–59。

## 三、40 局批测：他的截图就是中位数

`playOne(seed 1000..1039)`，取生涯结束那一刻：

| 指标 | 最小 | 中位 | 均值 | 最大 |
|---|---|---|---|---|
| 队友互相 · 最差一对 | 30.8 | **45.8** | 46.2 | 62.4 |
| 队友互相 · 均值 | 37.3 | **53.3** | 54.0 | 65.9 |
| 队友互相 · 最好一对 | 50.0 | 62.9 | 62.3 | 73.1 |
| `relMod()`（战力系数） | 0.9842 | **1.0036** | 1.0000 | 1.0177 |
| **他们对你的信任（对照）** | 75 | **100** | 96.6 | 100 |

- **信任中位数 100，关系中位数 53.3。** 玩家「跟我的都 100 了，他们之间还是一坨」是**每一局的常态**，不是他运气差。
- **`relMod()` 中位 1.0036 = 战力 +0.36%。** 这个系统对结果的影响四舍五入是零。
- 唯一有真实惩罚的档（最差一对 < 32）**40 局只命中 1 局**。
- 成就「没有裂痕」（任意两人 ≥60）**40 局只有 2 局达成**——不是难，是**没有办法**。

## 四、还有三处界面在承诺不存在的东西

| 位置 | 原文 | 实际 |
|---|---|---|
| 更衣室关系卡 `clout.ts:417` | 「**合练和团建能缓和**，连败会加剧」 | 合练（`squad.ts:175`）只调 `addTrustAll(1.8)`，**不碰 `S.rel`** |
| 赛后拆解 · 士气 `postmatch.ts:61` | 「处理好队友关系，或者**花钱团建**」 | 同上 |
| 商店 · 约队友吃火锅 `shop.ts:169` | 「体力和**关系**一起补」 | `shop.ts:679` 只调 `addTrustAll(7)` |

玩家按提示去做了，数字不动——这比「没有这个功能」更伤。

---

## 五、方案

### 甲、把已经存在的动作接上 `addRel`（必做，最小）

一个新的小工具 `relAll(n)`（全队每一对 ±n）＋ 现有动作各加一行：

| 动作 | 现在 | 加上 |
|---|---|---|
| 战队合练 `squad.ts:175` | 信任 +1.8 | **每对 +1.2**（成建制一起练，最能磨人际；卡面文案本来就这么写的） |
| 战队训练赛 `squad.ts:160` | 信任 +1.8 | **每对 +0.8**；那个 28% 的摩擦事件里，给**当事人和其他四人** −1.5 |
| 约队友吃火锅 `shop.ts:169` | 信任 +7 | **每对 +3**（说明文案本来就写着「关系一起补」） |
| 随机事件「开个会，把问题摊开讲」`random.ts:535` | 信任 +10 | **每对 +4** |
| 随机事件「去，喝两杯」`random.ts:584` | 信任 +7 | **每对 +3** |
| 媒体日「甩锅」`cer.ts:144` | 信任 −3 | **每对 −1.5**（队友互相也会传） |
| 更衣室吵架 `team.ts:117/119` | 信任 − | 对应那一对 **−4 / −8** |

**队友双排不接**——双排是「你和一个队友」，动的是他对你的信任，不是队友互相。
这条界限要守住，否则两个数值就糊成一个了。

### 乙、给玩家一个直接杠杆：「私下调解」（推荐）

现在替补/首发都没有任何一个「我去处理这件事」的按钮，玩家只能干看着。

- **私下调解**：1 个行动点，**每赛段一次**，出现在「队伍」栏最差那一对旁边
- 成功率看你的 **心态 / 指挥**（取高的那一维）和这一对现在的关系
- 成功：这一对 **+8**，全队每对 +1；失败：这一对 **−2**，「你插手得太早了」
- 威望（`cloutOf()`）高的时候成功率有加成——**队里说话有分量的人才劝得动**

这一条同时回答了玩家那句「怎么好像只有随机事件影响」。

### 丙、让它真的有分量（要批测）

现在 `relMod()`：

```ts
return 1 + ((mean-50)/900) + ((worst<32) ? -(32-worst)/700 : 0);
```

中位局 = **+0.36%**。建议：

```ts
return 1 + ((mean-50)/300) + ((worst<38) ? -(38-worst)/400 : 0);
```

| 局面 | 现在 | 改后 |
|---|---|---|
| 中位（均值 53.3） | +0.36% | **+1.1%** |
| 经营得好（均值 65） | +1.7% | **+5.0%** |
| 有一对闹掰（最差 25） | −1.0% | **−3.3%** |

搭配甲＋乙，「更衣室经营」才第一次有实际收益。
**这一条会动战力，必须 240 局批测**（联赛/世界赛夺冠率、生涯综合）。

### 丁、把说明改成实话（无论甲乙丙做不做，这条都要做）

- 更衣室关系卡：只有真的接上之后才敢写「合练和团建能缓和」；在那之前改成
  **「一起赢会拉近，连败、挂牌、买断会拉开」**——这是当前唯一为真的一句
- 赛后拆解 · 士气的 fix 文案同步
- 火锅的「体力和关系一起补」——接上甲之后为真，否则改成「体力和信任一起补」

### 戊、把两个数的量纲说清楚（顺手）

玩家的困惑有一半是**看到两个 0–100 的条挨在一起，以为是同一把尺**。
「他们对你的信任」有二十几个来源、很容易顶到 100；「队友互相」起点就是 42–61、只会慢慢动。
建议关系卡上加一行小字，或者干脆把队友互相改成**文字档位**（疏远 / 一般 / 熟 / 铁），
不给具体数字——**没有杠杆的数字不该给精确读数**。

---

## 六、推荐的打包

| 批次 | 内容 | 风险 |
|---|---|---|
| **第一批（建议现在做）** | 丁 + 戊 | 零——只改文案与显示 |
| **第二批** | 甲 + 乙 | 低——只增加正向来源，不动战力公式；批测看关系均值分布 |
| **第三批** | 丙 | 中——动战力，240 局批测把关 |

## 七、等作者拍板

1. 三批一起做，还是先上第一批（说实话）止血？
2. 「私下调解」（乙）要不要做？它是玩家那句话最直接的回应。
3. 队友互相的关系，**保留 0–100 数字**还是改成文字档位（戊）？
