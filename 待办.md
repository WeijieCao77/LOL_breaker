# 破局者 · 待办

交接给下一个 session。状态：代码全部已推送，线上是 `59891ac`。
线上地址 https://lol-breaker-production.up.railway.app

跑测试的命令在最后一节。

---

## 〇、2026-09-02 这一轮改完的（玩家六条反馈）

代码在工作区，**未提交**。构建：`PYTHONIOENCODING=utf-8 python demo/build.py`；自检 `npm test`；
40 局批测零崩溃（脚本在临时目录，思路：`require("demo/test.js").playOne(opts)` 循环跑，
统计 `scrims/trials/trialWins/mateInj/benchWeeks/maxRank`——这些字段 `playOne` 现在都返回）。

新模块 `demo/rotation.js`（build.py 已注册，占位 `__ROTATION_MODULE__` 在 injury 后面）：
· **赛程**：`fixNote()` 在 `endMatch`/`benchWeek` 记赛果到 `S.fix`；`fixtureStrip()` 挂在
  `nextMatchCard()` 尾部，`fixtureCard()` 在「世界」栏顶部；随队/替补看季后赛的间歇周用
  `setBreakAgenda()` 写日程、`breakAgendaCard()` 显示（intl.js `benchedIntl`、startPlayoff 替补分支）。
· **训练赛对位挑战**：`scrimPanel()`（替补页/间歇页）、`startScrim/scrimPick/endScrim`、遮罩 `scrimCard()`。
  行动点键 `duel`=2。赢攒 `S.scrim.edge`，≥3 置 `pendingTrial`，`isBenched()` 开头调
  `scrimTrialCheck()` → `grantTrial()` 把你换进名单；`endMatch` 调 `rotationAfterMatch(won,gap)`：
  赢→`confirmStarter()` 永久转正；输→场次用完 `revertTrial()`，资本减半；对手强 6 以上原谅一次。
  `proPerf()` 里替补按训练赛胜场加分（≤9）。
· **队友伤病**：`mateInjuryRoll/Tick` 挂在 `nextWeek` 和 `offNextWeek`；`S.mateInjury` 单例；
  `powerCore`/`squadBase` 对该队友 −5.5；受伤的是你的对位首发 → `grantTrial(周数,{injury:true})`。
· **国服第一**：`achieve_more.js` 的 `grandmaster` 阈值 95→99（原是从 top10 复制的）；`rankFull` 99+ 显示「国服第一」。
· **复盘**：`postmatch.js` `reviewAdvice` 先列长项（`good:true`），短板只算进战力公式的四维并按权重排，
  极端加点（五维 sd≥9）明说没有短板惩罚；`pmAdviceHtml` 赢球也显示长项。
· **门槛**：`tryout.js` `tierCard()` 插在转会页合同后、职业前转会页顶部；接触/自荐按钮带期望值；
  属性卡标题带综合（俱乐部口径）。

数值面还没细调的地方：训练赛成功率 `scrimOptP` = 0.52 + (你−他)/38 − (risk−0.8)×0.10，
新人对豪门首发普遍 12–40%（机器人 25% 胜率）；队友伤病 0.4%/人/周（老将 +0.2~0.4%）。

### 审计回填（2026-09-02，Codex 审计的十几条）

已修：新档 `scaleVer/born`（P0，读档误迁移 +15）、`fixScaleV2` 按世界均值加保险、导入存档 `sanitizeSave`、
server.js 重写（www 规范域名 301、`/xfer` 存档接力、gzip/brotli + ETag/304、CSP 用内联脚本 sha256 白名单、
HSTS/XFO/Referrer-Policy）、`.github/workflows/test.yml`（构建一致性 `demo/check_build.py` + npm test 断言 + 服务端冒烟）、
rankicon 四份 `tierArtKey`、模板/模块两份 `cupTick`、手机浮窗横排、favicon、README 成就数与地址、
`data/requirements.txt`、`fetch_oe.sh` 可移植、更新日志日期。

**没做 / 要你决定**：
· Railway：`railway.json` 2026-12-01 停止支持。`.railway/railway.ts` 已备好，但 `railway config pull/plan/apply`
  必须在项目所有者的 Railway 账号下跑（见 README「部署」）。builder 已改 RAILPACK。
· LICENSE 与真实战队名/队标/段位图标的授权边界——作者决定。
· 缺 3 个队标（RNG / Excel / BDS）：`data/fetch_logos.py` 依赖 `data/raw/teams_zh.json`（不进仓库）。
· 已经被误迁移过的存档（09-02 到今天之间新开且读过档的）无法可靠识别回滚——效果是玩家属性多了 15、世界被压回锚点，
  是增益不是损失；`fixSeasonAttr0` 只修了赛季成长显示为负的问题。

---

## 一、玩家明确提过、还没做的

按玩家提出的顺序，不是按优先级。

### 1. 属性面板要能常驻（玩家原话：做决策时忘了自己的强项是什么）

> 「我的能力值表格除了详细的表和能量条，我需要一个让我随时能看到的 pin 在页面里的
> 小数字表格，比如在页面左边或者就做成一个横条只有数字和名称的那种简洁版数据表」

现在五维只在「本周」tab 的属性卡里，切到别的 tab 就看不见了。
要做一条常驻的紧凑条：只有名称和数字，`position: sticky` 贴在 HUD 下面。
手机上是主场景，横条比左侧栏合适。

相关：`career_template.html` 的 `hud()`（约 2600 行）和 `attrCard()`。

### 2. 新事件要弹窗，不要出现在页面下方

> 「每次出来的新的事件有时候会在下面，我不滑动都看不见」

随机事件（`S.rndEv`）、更衣室（`S.locker`）已经是 `.rankup` 遮罩了。
出问题的是**内联卡片**：试训邀请 `inviteCard()`、转会问询 `proOfferCard()`、
杯赛卡 `cupCard()` —— 它们排在页面流里，页面滚下去就错过了。

两个做法，建议都做：
· 把「需要你做决定」的卡片改成遮罩（试训邀请、转会问询）
· `render()` 里检测到新的阻塞项时 `window.scrollTo(0,0)`

### 3. 主动发起转会申请

> 「我要能主动发出转会申请」

现在只有被动：休赛期 `rollProOffers()` 摇别的队来问。
要加玩家主动的那一侧——在休赛期挂牌自己，然后有队回应或者没人理。
可以复用 `proPerf()` 判断有没有人接，用 `cloutOf()` / 违约金决定成功率。

相关：`tryout.js` 的「五、转会」那一节。

### 4. 直播收入要走真实流程，不要在后台改数字

> 「不同阶段的名气带来不同的直播收入不同这个地方不应该直接就在后台改变数值，
> 要变成合理的符合现实的流程比如礼物收入分成提高，或者直播平台找我谈是否愿意
> 签约独家，然后签约和不签约要有不同的效果各有优点让玩家选择」

现在 `streamIncome()` 直接按人气算钱，玩家没有参与感。
要做成事件：人气到档 → 平台来谈 → 签独家（保底高、但可能限制某些行为）
或不签（自由、收入随人气浮动）。两条路各有优点。

相关：`shop.js` / `career_template.html` 的 `streamIncome()`。

### 5. 国际赛之间要有间隔和文字日志

> 「打完了 rng 季后赛……直接进入了世界赛打 ns……中间也是毫无休息，
> 这里至少得要空好几周，包括其中应该要有文字版的日志告诉我每天都发生什么了
> 比如抽签结果，小组分配之类的」

资格判定的 bug 已修（季后赛输了不再去世界赛）。**但两件事没做**：
· 季后赛结束到世界赛之间没有间隔周（`enterBreak` 已经支持多种去向，
  加一个 `next:"intl"` 即可）
· 没有抽签、小组分配这类过程日志。`intl.js` 里 `openIntl()` 生成分组时
  应该 `pushEvent` 出来。

### 6. 确认「交易队友」功能还能不能用到

> 「我以前设计的能够出动将队友交易的功能还在吗」

代码**在**：`clout.js` 有 `canList/doList/signTargets/doSign`，威望/教练信任/经理信任
三个前置也都在。但我**没有验证界面上能不能走到**——需要确认 `cloutCard()`
在赛季里可见，且挂牌/签人的按钮有绑定。

---

## 二、我发现但没动的问题

### 7. 生涯结束时人气几乎人人到顶

实测中位数 730，档位表最高档是 850「这个项目的门面」。
意味着上面三四档基本没有区分度。人气主要来自战绩不是直播，
所以这是打比赛的累积问题，不是直播的问题。

### 8. 一局时长仍然只有目标的一半

实测 763 个决策点 ≈ 51–102 分钟，目标是 2–3 小时。
而且构成有问题：43% 的点击是「训练与行动」，属于重复操作。
加周数只会更枯燥，要加的是分叉。玩家说过「时长先不管」。

### 9. 单局只看得到约 30/72 成就

设计上靠多周目，但测试者大概率只玩一局。

### 10. 转会费的完整市场没做

违约金现在只影响「别队来挖你的概率」，转会时由买方支付（有日志）。
但没有真正的转会市场（别的队之间互相交易、报价竞争）。
玩家之前说过「转会费先不做」。

### 11. 受数据限制的两件事

· 指挥值与战队实力仍有 +0.52 相关。指挥 = 胜负贡献 − 个人数据，
  用单年数据拟合，强队选手天然被高估。要压下去需要多年数据做选手固定效应。
· 2024–2026 的真实名单从没下载过，S13 之后的世界是推演的。

---

## 三、这一轮改完的东西（新 session 需要知道的上下文）

免得重复劳动或者误判。

**新系统**
· 试训与合同谈判（四天评估 → A+~D 评级 → 年薪/签字费/年限/违约金谈判）
· 转会：休赛期别的队来挖，含外赛区；违约金影响被挖概率
· LDL 二级联赛：青训合同真的签进 LDL，有升队通道
· 可玩休赛期（3 周）、季中间歇（2 周）、季后赛前休整（1 周）
· 淘汰赛每轮之间的备战环节（1 个行动点）
· 存档（自动存档、继续上次、导出导入、版本迁移）

**几个反复出现的坑，模式值得记住**

这一轮修的 bug 里有很大一部分是同一种：**代码写好了，但没有任何地方调用它。**

· `relMod()` —— 注释写着「有一对闹掰整队都别想顺」，但没人调，队友关系是纯装饰
· `BREAK_PATHS.心态/指挥` —— `done()` 写死 `false`，进度条数到 3/3 却永远不突破
· `langBonus()` —— 只在「已经身处 LCK」时生效，而去外赛区几乎不可能，韩语课买不回本
· `AP_OFF` —— 常量和 `apFor("off")` 都写好了，但从来没被调用，休赛期是一个按钮
· 队标 —— 72 个图片躺在 `data/logos/`，`export_game.py` 里没有处理 logo 的代码

**改数值之前先量，不要用公式糊弄。**
玩家两次报「操作对了却输了」，我都是先跑真实代码统计，而不是算公式：
一次证明是 0.9% 的小概率（机制没错，但界面没给赢面，所以像 bug）；
一次是真 bug（`clampWinProb` 用的是不含临场决策的差距）。

**加内容会引发数值通胀，必须复测。**
间歇期 + 备战让一年多出 30+ 个行动点，MSI 冠军率被抬到 45%、双冠 20%。
回调了三处才回到基线（19%/9%/5%）。以后每加一段可玩内容都要跑一次
`batch.js` 看结局分布。

---

## 四、怎么跑测试

脚本在临时目录（会被清理），需要的话重写。它们做的事：

· `t_new.js` —— 存档、计划游标、自动推进、杯赛赛程的专项断言
· `t_tryout.js` —— 试训邀请概率、四天评估、评级分布、谈判、签字落地
· `batch.js` —— 60 局全生涯，看完成率、崩溃数、结局分布
· `byrank.js` —— 按段位验收杯赛夺冠率
· `len.js` —— 统计一局的真实决策点数，折算时长

构建：

```bash
PYTHONIOENCODING=utf-8 python demo/build.py          # 产出 demo/career.html
PYTHONIOENCODING=utf-8 python demo/build_xhs.py      # 小红书小工具包
```

Windows 上 `python` 的 stdout 是 cp1252，打印中文会崩，所以要带
`PYTHONIOENCODING=utf-8`。写文件本身不受影响——**注意：脚本如果在
`print` 上崩了，前面的写入其实已经生效了**，别以为没改成功而重复执行。

推送要在前台跑，后台 shell 读不到 GitHub 凭据、而且会返回退出码 0，
看起来成功其实失败了。

---

## 五、发布状态

· **B 站 Toy**：已提交审核，id `26845865242624`，slug `the-breaker`，
  仅链接可见。通过后是 `bilibili.com/toy/the-breaker/index.html`。
  更新用 `toy update 26845865242624 demo/career.html`（两段式，
  不加 `--yes` 只出预览）。
· **小红书小工具**：包能构建（`demo/破局者-小红书小工具.zip`），
  官方自检 23 项全过，但**投稿入口还没找到**——官方那份文档只讲打包规范，
  不讲怎么提交。
· **Railway**：推 main 自动构建，几分钟生效。

完整的 B 站发布流程和踩过的坑记在 `B站Toy发布流程.md`。
