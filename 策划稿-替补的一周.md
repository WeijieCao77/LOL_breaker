# 策划稿 · 替补的一周

**状态**：等作者审批。**代码一行没动**（作者原话：「不要直接动代码，先做问题总结和解决策划案」）。
**起因**：玩家反馈（2026-09-09）——

> 我一个臭替补，试训 B 合同，凭什么去接受采访放狠话或者说其他的。
> 还有我都替补了，首发阵容磨不磨合那是他们的事，为什么要花我的行动点，
> 当我不在首发的时候，我觉得这个他们自己磨合自己升不就好了。

两句话指的是两个不同的东西：一个是**媒体日**（`cer.ts` 的 `media` 仪式），
一个是**战队行动**（`squad.ts` 的 `SQUAD_ACTS`）。查下来**两条都成立，而且都不是显示问题**。

---

## 一、他说的两件事，代码里都对得上

### 1.1 媒体日对替补没有任何门槛

`main.ts:2699` 的 `startSeason()`：

```ts
if(S.career&&(S.homeLeague||"LPL")!=="LDL"){
  if(S.extended&&S.si===lastSeason()&&S.split===0) cerStart("farewell0");
  if(S.split===0) cerStart("patch");
  cerStart("media");          // ← 没有任何身份判断
}
```

唯一的门是「不在 LDL」。所以**一个签着 B 档替补合同、一场正赛都没打过的人，
每个赛段照样被三个记者架在背景板前**，还能从三个口径里挑：

| 口径 | 效果（`CER_EFF.media`） |
|---|---|
| 狂（放狠话） | 热度 +15，这个赛段**输了心态伤害 ×1.5** |
| 稳 | 热度 +5 |
| 甩锅 | 热度 +10，**更衣室信任 −3** |

题目本身也是按首发写的（`mediaQuiz()`）：

- 「上赛段你们……这个赛段目标是什么？」选项里有「一场一场打，**先把首发坐稳**」——他连首发都不是
- 「和 ${队友} 的**配合磨合**得怎么样？」——他不上场，没有配合可磨
- 「${对手} 说这个赛区没人能和他对线」→「那他来试试」——他跟谁对线？

**顺带挖出一个白嫖**：「狂」的代价 `mediaTiltMul()` 只在 `main.ts:3979` 的
`endMatch()` 里被读到。替补走的是 `benchWeek()`，**根本不进 `endMatch`**。
也就是说 —— **替补选「狂」，热度 +15，零代价**。这是当前版本最便宜的热度来源之一。

### 1.2 战队行动花你的点，喂的是你没份的池子

`SQUAD_ACTS` 四个行动，全部从 `S.ap` 扣：

| 行动 | 行动点 | 体能 | 落到哪儿 |
|---|---|---|---|
| 训练赛 | **3** | −20 | 默契 +5.1、战术 +4.5、全队信任 +1.8 |
| 战术复盘 | 2 | −9 | 战术 +4.2、**运营 +0.35**、**攒运营突破条** |
| 战队合练 | 2 | −13 | 默契 +4.4、全队信任 +1.8 |
| 队友双排 | 1 | −4 | 默契 +1.2、**全队信任 +1.9**、**操作 +0.14** |

默契 / 战术（`S.squad.syn / tac`）唯一的去处是 `powerCore()` 里的

```ts
const sq=(1+(syn-50)/950)*(1+(tac-50)/1100)*rm;
```

—— **队伍战力**。而替补周的比赛是 `benchWeek()` 用 `power(myRoster(),0,fav)` 模拟的，
`myRoster()` **里没有你**（你在 `S.understudy` 的后面）。赢了：`addFans(-1.5)`、
「你在替补席上看完了整场，没人提你的名字」。季后赛更直白：`S.benchedPO=true`，
**夺冠不进你的生涯表**。

所以玩家的算术是对的：**替补掏 3 点行动点打训练赛，买到的是一支他拿不到战绩、
拿不到冠军的队的战力**。这不是「他不懂设定」，这是设定确实少写了一条。

（`S.squad` 还每赛段向 50 回归 `SQUAD_DECAY=0.21`——不喂就掉。等于替补被这个池子
持续抽血。）

---

## 二、这条线游戏自己已经有了，只是没铺完

替补该被排除在「首发才有的场合」之外，**代码里已经有三处先例**：

| 场合 | 现状 | 出处 |
|---|---|---|
| 年度颁奖夜 | ✅ 已排除 | `cer.ts:250` `const bench=isBenched(); … if(p.me&&bench) return;` |
| 季后赛抽签仪式 | ✅ 已排除 | `main.ts:4245` `if(isBenched()){ …模拟…; return; }`，早于 4279 的 `cerStart("draw")` |
| 世界赛出征仪式 | ✅ 已排除 | `intl.ts:178` `if(isBenched()&&S.understudy) return benchedIntl(...)` |
| **媒体日** | ❌ 没门槛 | `main.ts:2701` |
| **战队行动** | ❌ 没门槛 | `squad.ts:280` `squadActs()`，渲染在 `main.ts:3513 / 4424 / 4887` |

顺手把整个仪式面都过了一遍，另外两个的结论是**维持原样**：

- **版本发布会**（`patch`）：教练组开会念版本变化，替补也在训练室里。给的是
  「整赛季版本相性 ±」，转正之后照样吃得到。**合理，不动。**
- **全明星**（`allstar`）：`allstarSelected()` 要一阵容 / MVP / 冠军 / 粉丝 ≥900。
  替补每周 `addFans(-1.5)`，够不着。**自然筛掉，不动。**

---

## 三、替补的一周现在长什么样

赛季周 `AP_SEASON=8`。把当前所有能点的东西按「对一个替补有没有意义」摊开：

| 行动 | 点 | 对替补的意义 |
|---|---|---|
| 训练（五维） | 2 | ✅ 五维均值压过首发 −2 就能直接转正 |
| 打排位 | 1 | ✅ 状态、手感 |
| 直播 | 2 | ✅ 钱、人气（还能抵消每周 −1.5 的掉粉） |
| 做内容 | 2 | ✅ |
| 休息 | 1 | ✅ |
| **训练赛对位挑战** | 2 | ✅✅ **唯一的上升通道**——攒满 3 点轮换资本换一场正赛试用 |
| 战队 · 训练赛 | 3 | ❌ 纯喂 `S.squad` |
| 战队 · 战术复盘 | 2 | ➗ 战术池 ❌，但**运营 +0.35 和突破条是他自己的** |
| 战队 · 合练 | 2 | ❌ 纯喂 `S.squad` |
| 战队 · 双排 | 1 | ➗ 默契 ❌，但**信任 +1.9、操作 +0.14 是他自己的** |

对位挑战有限次（`rotation.ts:116`：**每周最多 2 次、连续两周最多 3 次**，玩家 2026-09-06
自己提的），所以每周稳定占 4 点，**剩下 4 点**。剩下这 4 点其实是有地方去的
（训练 / 排位 / 直播），**不是没事干**——但四个战队按钮就摆在同一屏的正中间，
点了才发现买的是别人的东西。**这是个陷阱按钮，不是一个选择。**

---

## 四、方案

### 甲、媒体日：替补不上背景板

| | 做法 | 代价 | 风险 |
|---|---|---|---|
| **甲-1（推荐）** | `cerStart("media")` 前加 `!isBenched()`，和颁奖夜同一套写法 | 一行 | 替补少一段戏；顺手**堵掉「狂」白嫖热度** |
| 甲-2 | 保留仪式，换替补版题库：一题、只有「稳 / 狂」、热度减半，「狂」的代价改成**教练组盯得更紧**（下一次对位挑战成功率 −5%） | 一个新题库 + 一条新效果 | 要新平衡；但替补线多一段属于他的戏 |
| 甲-3 | 只改文案，效果不变 | 最小 | **不解决问题**——他还是在放狠话 |

**推荐甲-1 先上线**（一行、零风险、当场解决玩家的不适，并且关掉白嫖），
把甲-2 记进待办当内容补充。理由：他抱怨的核心是「凭什么是我」，而不是「这段戏不好玩」。

### 乙、默契：替补期间首发自己磨合

玩家原话就是方案：「**他们自己磨合自己升不就好了**」。

| | 做法 | 风险 |
|---|---|---|
| **乙-1（推荐）** | ① `squadActs()` 里 `if(S.career&&isBenched()) return ""`（`doSquad()` 同步拦一道，防托管绕过）<br>② 替补期间 `S.squad` **自己走**：`benchWeek()` 里每周 `syn += (T−syn)×0.10`、`tac += (T−tac)×0.08`<br>③ 整赛段替补的赛段**跳过** `squadDecay()`（`S.benchedThisSplit` 这个标记已经有了）<br>④ 只有个人收益的两项**拆出来单独留着**（见下） | 队伍默契自走会让替补赛段的队伍略强 → 要批测 |
| 乙-2 | 按钮留着但改成 0 行动点、收益减半 | 还是替补在替首发交学费，**没解决他说的事** |
| 乙-3 | 什么都不改，只把说明文案写清楚 | 陷阱按钮变成写着「这是陷阱」的按钮 |

**`T`（这支队没有你也该有的默契水位）**：

```
T = clamp(50 + (本队胜率 − 联赛中位胜率) × 30, 46, 68)
```

强队自己越练越顺，鱼腩队原地打转——和「默契回归 50」比，这个口径更像现实，
也正好接住玩家那句「他们自己磨合自己升」。

**④ 拆出来保留的两项**（这一步很重要，不然替补反而少了两笔本来属于他的收益）：

| 原行动 | 替补版 | 点 | 体能 | 效果 |
|---|---|---|---|---|
| 战术复盘 | **一个人过录像** | 2 | −9 | 运营 +0.35、攒运营突破条，**`S.squad.tac` 不动** |
| 队友双排 | **队友双排**（原样） | 1 | −4 | 全队信任 +1.9、操作 +0.14，**`S.squad.syn` 不动** |

转正的那一刻，你继承的是首发这段时间自己磨出来的池子，然后照常吃
`disruptSynergy()` 的换人冲击——「新人顶上，磨合期重来」，这条链是通的。

### 丙、替补的一周该有的东西（本轮不做，先记账）

甲、乙 解决的是「不该有的」。「该有的」是另一件事，建议单独排期：

- **看录像**（1 点）：坐替补席看首发打完这场。战术素养 +0.2、**轮换资本 +0.3**
  ——教练看得见谁在记笔记。给替补一个「不打比赛也在往上走」的低强度选项。
- **找教练聊**（1 点 / 赛段一次）：把轮换资本的判据、他还差什么，明说一遍。
  替补线现在最缺的是**信息**，不是数值。
- **不建议**动对位挑战的周上限——那个 2 次 / 周是玩家自己 2026-09-06 提的。

---

## 五、真要动的时候，动这几处

| 文件 | 位置 | 改什么 |
|---|---|---|
| `main.ts` | 2701 | `cerStart("media")` → `if(!isBenched()) cerStart("media")` |
| `squad.ts` | 280 `squadActs()` | 职业生涯里 `isBenched()` 时返回替补版（收起四个，露出拆出来的两个） |
| `squad.ts` | `doSquad()` | 入口再拦一道（托管 / 存档回放 / 老 UI 的兜底） |
| `squad.ts` | `squadDecay()` | 替补赛段跳过 |
| `main.ts` | `benchWeek()` | 加 `benchSquadDrift()` |
| `demo/test.ts` | — | 三条自检：替补赛段不弹媒体日 / 替补看不到四个战队行动 / 替补赛段 `S.squad` 不向 50 掉 |

渲染点有三处（`main.ts:3513` 本周、`4424` 备战周、`4887` 休赛期）＋职业前车队那处
（`5338`），**门要下在 `squadActs()` 自己身上**，别在三个调用点各写一遍；
职业前（`!S.career && S.pre`）不受影响。

---

## 六、平衡与批测口径

乙-1 会让「替补赛段的队伍」变强一点（默契自己往 T 走，而不是掉向 50），
需要 240 局同种子批测盯这几个数：

- **转正率 / 转正周数**（替补线的核心体验）
- **下放 LDL 触发率**（队伍变强 → 首发位更难抢 → 下放会不会变多）
- 联赛冠军率 / 世界赛冠军率（别顺手把整体难度拉低了）
- 生涯末年综合、生涯总收入（媒体日少一次「狂」，热度会少一点）

甲-1 单独看基本无风险：影响面只有 `S.heat` 和 `S.media`，且**只在替补期间**。

---

## 七、需要作者拍板的三件事

1. **媒体日**：走甲-1（一行屏蔽，顺手堵白嫖），还是直接做甲-2（替补版一题）？
2. **默契**：走乙-1（替补期间首发自己往 `T` 磨合），还是更保守的「替补期间冻结、不涨也不掉」？
3. **丙那一块**（看录像 / 找教练聊）：这轮一起做，还是先只上甲、乙？
