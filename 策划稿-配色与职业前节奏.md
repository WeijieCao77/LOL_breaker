# 破晓 · 配色自选与职业前节奏 修改方案

2026-09-06。依据：讨论群玩家「冷水鱼616」的两条意见，以及作者的回复。

> 感觉培养有点冗长 —— 玩了三个档的第一感受 —— 建议培养稍微短一点
> 还有就是界面亮色看起来会舒服一点 —— 感觉界面可以借鉴一下隔壁 vct 电竞经理的
>
> （作者）培养是指加入联赛前的那段吗？ —— 对的 —— 界面应该是会出多个配色可以自选

两件事分开写。配色参考的是 `WeijieCao77/Val_Manager` 的 `src/styles.css` / `src/ui/theme.ts` / `src/ui/ThemeToggle.tsx`
（本地 `~/esport-manager`），那边已经做过一模一样的事：群里有人说黑底看久了头晕，于是加了浅色和米色两套。

---

## 一、配色：三套地面，可自选

### 1. VCT电竞经理是怎么做的（可以直接抄的部分）

| 做法 | 细节 |
|---|---|
| 三套地面，一个游戏 | 深色（默认，「赛事转播那种黑底」）· 浅色（白面板压冷灰，「像一张数据表」）· 米色（米黄底，「比白底柔和，看久了更舒服」，Kindle 的思路） |
| 全部 token 化 | 界面里每一个颜色都是 `--bg / --panel / --line / --text / --muted / --accent / --win / --warn / --loss…` 之一；组件里剩下的字面量只有卡片——「卡片是实体，在任何光线下都长一样」 |
| 浅色不是反色 | 深色下亮的 `--win #3dd68c` 在白底上只有 2:1，直接消失；浅色版每个彩色文字都加深到面板上 **≥ 4.5:1**（`#117a4a`） |
| 强调色与语义色分开 | `#FF4655` 只给品牌、主行动、「你的队」；胜 / 警 / 负各有自己的色，输一场永远不会看起来像品牌 |
| 切换控件 | 顶栏「深 / 浅 / 米」三段（每段带一块地面色样，本身就是预览）；手机上只放一颗，点一下换下一套；存档页有带说明的完整版 |
| 记在设备上 | `localStorage`，不进存档——「手机在太阳底下和半夜的显示器要的答案不一样」 |
| 不闪黑 | `index.html` 首屏前一行脚本读同一个 key，浅色页面不会先黑一下再变白 |
| `theme-color` 跟随 | 换主题时改 `<meta name="theme-color">`，手机浏览器的地址栏颜色一起换 |

### 2. 破晓的现状

- `demo/theme.css` 的 `:root` **已经是 token 化的**：`--void / --deep / --panel / --panel-2 / --line / --line-hot / --gold / --gold-hi / --gold-dim / --cyan / --red / --dawn / --ink / --ink-2 / --ink-3`，
  加五个功能域色 `--z-act / --z-me / --z-team / --z-world / --z-money / --z-rec / --z-auto`，还有一块「幽灵变量别名区」。基础比 VCT 那边动手时好。
- 但散落着不少写死的 `rgba(...)`：HUD 渐变、卡片底、`.toast`、`.tour`、`.rankup` 遮罩、赛后表格斑马纹、`.diary` 底……这些在浅底上会变成一块块深斑。
- `<meta name="color-scheme" content="dark">`、`theme-color #0B1220` 都是写死的。
- 主视觉封面（`/img/keyart.jpg`）、结局名片 `.poster`、卡片切角 `clip-path` 是身份，不随主题变。

### 3. 破晓浅色 / 米色的建议值（已算对比度）

原则：**保留破晓的身份色（金 / 青 / 晨光橙 / 红），只把它们压深到白底上读得清**；不换成 Valorant 的红。

| token | 深色（现） | 浅色建议 | 白底对比 | 米色建议 | 米底对比 |
|---|---|---|---|---|---|
| `--ink` 正文 | `#E8F0F6` | `#14202B` | 16.5 | 同左 | 15.3 |
| `--ink-2` 次要 | `#9DB8C9` | `#3F5468` | 7.8 | 同左 | 7.3 |
| `--ink-3` 说明字 | `#7395AA` | `#5B6F84` | 5.2 | 同左 | 4.8 |
| `--gold` 标签 / 已得 | `#C9A961` | `#8A6A1C` | 5.1 | `#6B5218` | 6.8 |
| `--cyan` 正向 / 你 | `#4FBEDA` | `#146F86` | 5.8 | 同左 | 5.3 |
| `--dawn` 作文字用 | `#E9A15A` | `#A65814` | 5.2 | 同左 | 4.8 |
| `--dawn` 作按钮底 | 不变 | 不变（按钮字 `#071626` 在橙上 8.4） | — | 不变 | — |
| `--red` 损失 | `#C4535F` | `#B0303C` | 6.3 | 同左 | 5.8 |
| `--z-team` | `#5FA88B` | `#1F7A57` | 5.3 | 同左 | 4.9 |
| `--z-world` | `#8B93D9` | `#4A52B8` | 6.6 | 同左 | 6.1 |
| `--z-money` | `#D89B4A` | `#9A5F0F` | 5.2 | 同左 | 4.8 |
| `--z-rec` | `#7E93A6` | `#4E6478` | 6.1 | 同左 | 5.7 |
| 地面 `--void / --deep / --panel / --panel-2` | 深蓝四阶 | `#EEF1F5 / #E6EAF0 / #FFFFFF / #F3F5F8` | — | `#F1EADC / #E9E1D0 / #FAF6EC / #F1EADB` | — |
| `--line / --line-hot` | `#1E4A63 / #2F6E8C` | `#D5DCE5 / #AAB8C8` | — | `#D6CBB3 / #B3A486` | — |

对照：现在的深色 token 直接放到白底上——金 2.3、青 2.2、橙 2.2、`--ink-2` 2.1、`--ink-3` 3.2——全部不及格，这就是「不能简单反色」的原因。

米色那一列的 `--gold` 用了更深的 `#6B5218`：金在米黄底上最容易糊，其它色差不多够，先不动。

### 4. 改动清单（按顺序）

1. **收 rgba**：把 `theme.css` 里写死的 `rgba(…)` 归到新增 token（`--wash-gold / --wash-cyan / --wash-red / --scrim / --shadow-card / --stripe`），
   `card` / `toast` / `tour` / `rankup` / `diary` / `bxt` 斑马纹 / HUD 渐变一处一处过。这一步不改任何视觉，只是让后面两套能覆盖。
2. **两块覆盖**：`[data-theme="light"]` / `[data-theme="cream"]` 各写一块 `:root` 级覆盖，值取上表；`color-scheme` 跟着切。
3. **不换的东西列清单**：结局名片、生涯名片截图、主视觉封面、段位徽章、成就弹窗的奖杯——照 VCT 的「卡片是实体」原则，深色地面**保留在这些块内部**（它们自带深色底，不是透明的）。
4. **切换控件**：属性条工具区（现在有「数值 / 导览」）加「深 / 浅 / 米」三段，每段带色样；手机 ≤ 560px 只放一颗循环键（那一栏本来就挤，见 v20260906z 的 320px 修补）；「教程说明」栏目里放带说明的完整版。
5. **首屏不闪**：`career_template.html` `<head>` 里加一行同步脚本读 `poxiao_theme`，设 `data-theme` 和 `theme-color`；`build` 产物一起带。
6. **记在设备**：`localStorage` 键 `poxiao_theme`，不进存档、不进 `/xfer` 搬家。
7. **验证**：
   - 一段脚本把三套 token 对（前景 × 面板 / 地面）的对比度全算一遍，低于 4.5 的直接失败，进 `npm test`；
   - `test-ui.ts` 加：切主题后 `data-theme` 正确、刷新后记住、`theme-color` 跟随；
   - 三套各截职业前 / 赛季 / 结局三屏，桌面 + 手机，人工看一遍斑块和糊字。

工作量：第 1 步是大头（把 rgba 收干净，半天到一天），2–6 合计一天，验证半天。

### 5. 「借鉴 VCT 电竞经理界面」还能借什么（配色之外）

玩家说的是「看起来舒服」，除了地面亮度，那边还有三条规则值得拿过来，都不需要改结构：

- **一种强调色只做一件事。** 破晓已经这样做了（晨光橙 = 「该你了」，金 = 已经拿到的），保持住，浅色版也不要让金色去当按钮。
- **语义色和身份色分开。** 破晓的红只表示损失、青只表示正向——对。检查一下浅色版里 `--z-money` 琥珀和 `--dawn` 橙是否会混（两者都是暖橙）；必要时 `--z-money` 往棕走。
- **顶栏工具用字不用图标。** 「深 / 浅 / 米」三个字比太阳 / 月亮图标清楚，因为第三个选项出现之后图标就说不清了。破晓属性条的「数值 / 导览」已经是这个路数。

不建议抄的：Valorant 红 `#FF4655` 和它的左侧栏。破晓的 Hextech 金青是自己的身份，换掉就成了另一个游戏。

---

## 二、职业前节奏：为什么「培养冗长」，怎么缩

### 1. 现状（源码里的数）

| 项 | 值 | 出处 |
|---|---|---|
| 一年多少周 | **20** | `PRE_YEAR=20`（`demo/src/main.ts:1717`） |
| 每周行动点 | **12** | `AP_PRE=12`（`main.ts:484`） |
| 行动成本 | 排位 / 休息 / 网吧 / 换游戏 / 看录像 1 点，训练 / 直播 / 代练 2 点 | 行动卡 |
| 每周点击 | 6–10 次行动 + 1 次推进 | — |
| 一年点击 | **≈ 150–220 次** | 推算 |
| 最早可能上岸 | **第 13 周** | `PRE_EARLIEST=13`「主播杯打完之前，没人会下判断」 |
| 里程碑 | 第 8 周城市争霸赛 · 9 青训私信 · 14 主播杯 · 15 教练到场 · 17 全明星表演赛 · 17–20 转会窗口 | `PRE_MILESTONES` |
| 机器人几年上岸 | 0 个整年（第一年内） | `demo/test.ts` `preYears` |
| 真人 | 常见 1–2 年 | 我的实测：第一年自动推进到年底没签，第二年第 4 周签 IG.Y |

也就是说：**一个玩家在「真正的游戏」（赛季）开始之前要点 150–400 次**，而前 13 周无论怎么玩都不可能上岸。
第一档还有新鲜感——出身卡、段位、第一次杯赛；第二档、第三档起，这 13 周就是纯重复。玩家「三个档的第一感受」说的就是这个。

再看内容密度：20 周里只有 6 个里程碑，其中 2 个是纯日期（窗口开 / 关），中间有大段「没有事发生」的周——
`random.js` 的际遇事件大多是赛季里的。空周越多，冗长感越重。

### 2. 方案（分层，可以组合）

**方案 ① 老玩家快速起点（最便宜，先做）**

建档页「出身」之后加一项「起点」：

- 从零开始（现在的样子）
- **路人王起点**：从第 12 周开始，段位、人气按出身给到「城市争霸赛刚打完」的水平，跳过前 12 周
- 只对**通关过一次**（`localStorage` 有结局记录）或**本机第二个档**开放；导览和第一档不受影响

改动点：`startPre()` 接一个起点参数；`S.pre.week`、`S.pre.rank`、`S.fans`、`S.heat` 按出身预设；
里程碑表按周号自然跳过；`test.ts` 加一条从第 12 周开始也能跑完的用例。一天。

**方案 ② 把年压短（治本，要批测）**

`PRE_YEAR 20 → 14`，里程碑重排：城市争霸赛 5 · 青训私信 6 · 主播杯 9 · 教练到场 10 · 全明星 11 · 窗口 11–14；
`PRE_EARLIEST 13 → 8`；`AP_PRE 12 → 10`。

**必须同步校准**：训练 / 排位每周收益按 20/14 上调，`preScore` 门槛不动，目标是「一年内上岸概率」和现在持平。
校准工具现成：`demo/test.ts` 跑 60 局，统计上岸周数的 p50 / p90 和上岸时段位分布，改前改后对比。
不校准就等于把上岸难度提高 30%，那是另一个 bug。两到三天（大头是批测）。

**方案 ③ 第二年不重头（和 ② 独立）**

没上岸的第二年，从主播杯前一周开始而不是第 1 周（`preNextYear()` 里 `P.week=1` 改成 `PRE_EARLIEST-1`），
段位按现在的规则 −4，文案「你又练了半年」。第一年的 1–12 周只经历一次。半天。

**方案 ④ 空周填事（内容，不是数值）**

职业前专属际遇：网吧偶遇、直播间黑粉、代练单子、队友退队、教练试探、前职业的弟弟被拿来比较（出身卡的故事线）——
每周至少一件小事，`random.js` 加一张职业前事件表，`fireWeekEvents` 里按 `S.step==="pre"` 抽。
不改节奏也能明显减轻冗长感，但工作量最大（写文案）。一周。

**方案 ⑤ 让已有的「自动推进」被看见**

v20260906y 已经有「自动推进到年底」，玩家显然没用到。第 2 周起在行动卡顶上放一句：「不想一周一周点？自动推进到年底 →」，
导览里那一步也已经有。零成本，但它是绕开问题，不是解决——只做这一条玩家会觉得「原来作者也知道这段没意思」。

### 3. 推荐组合与顺序

1. **③ + ①**（一天半）：第二年不重头、老玩家快速起点。立刻消掉「三个档都要点 13 周」这件事，风险最小。
2. **②**（两三天）：压到 14 周，用批测把上岸率校回去。这是玩家真正要的「稍微短一点」。
3. **④** 排在后面，和「赛场外与留存」那份稿的事件扩充一起做。
4. ⑤ 顺手加，不单独算。

### 4. 验证

- `test.ts` 输出加 `signWeek`（上岸时的年 × 周），跑 60 局出 p50 / p90；目标 **p50 ≤ 12 周、p90 ≤ 1 年 + 主播杯**。
- 后台加一个信标「第一次签约」（现在只有 `career`，看不出是第几周签的），上线一周后看真人的分布。
- 群里回一句「职业前缩到 14 周、第二个档可以从路人王起步」，让提意见的人自己验。

---

## 三、给群里的回复（草稿）

> 两条都记下了。配色会做「深 / 浅 / 米」三套，和隔壁一样在顶栏切，记在手机上不进存档；
> 职业前会压到 14 周，第二个档起可以直接从「打完城市赛的路人王」开始，不用再点前面那半年。
