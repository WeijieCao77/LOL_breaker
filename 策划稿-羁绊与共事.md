# 策划稿 · 羁绊与共事：把已经发生的那条弧线说出来

**状态**：等作者审批。代码零改动。
**起因**：作者 2026-09-09 的原话——

> 赛事内可以跟队友、教练搞点羁绊、搭档的交流选项，更有代入感和情感共鸣。
> 我在旧档 RNG 的时候就觉得，刚去的时候四个大哥带我一个，大哥老了我带一绿带四红硬带他们夺冠，
> 后面来了新人，我也带着他们一点点成长夺冠，最后我要退役了，小弟们的综评已经超越我了，
> 开始带飞我了，就会有的感触。

---

## 一、先说一个好消息：这条弧线**已经在数值里真实发生了**

作者描述的四个阶段，引擎每一步都在跑：

| 阶段 | 谁在跑 | 出处 |
|---|---|---|
| **被带**：刚去，四个大哥带你一个 | 新秀赛季 `ROOKIE_MALUS`，你比队友弱是真的 | `powerCore` |
| **大哥老了**：他们掉，你顶上来 | `ageWorld()` 每年按 `ageCurve` 给 ≥24 岁扣属性，30 岁（或 27 岁且综评 <47）退役换新秀 | `main.ts:1682` |
| **一绿带四红**：你扛着他们 | 每一场都算你和队友的评分差 —— `meGap`、`mateAvg`、`carry`（院长局）、`soloWin`（一人成军） | `boxscore.ts:72` |
| **新人长起来**：带着他们夺冠 | `ageWorld()` 给 ≤23 岁的队友每年 `dev=+2.3`，各自有 `ceil` | `main.ts:1701` |
| **他们超过你**：小弟带飞你 | 你 24 岁起走 `ageCurve` 下坡，他们还在 +2.3 —— 交叉是必然的 | `main.ts:974 / 1701` |

**所以不缺机制，缺的是三样东西：记忆、认出、表达。**

## 二、坏消息：队友一走，游戏把你和他的一切删干净

`team.ts` 的 `syncTrust()`：

```ts
Object.keys(S.trust).forEach(k=>{ if(!ids.includes(k)) delete S.trust[k]; });
```

**队友不在当前名单里，那条信任直接 `delete`。** 加上 `S.trust={}` 在四处被整个清空（转会、下放、自由身、车队），结论是：

- 一起打了三年的大哥退役那天，你和他之间的一切**归零，不留痕迹**
- 你带过的新人被卖到别的队，下赛季你们在赛场相遇，游戏**不认得他**
- 生涯结束时，游戏说不出「和你并肩最久的人是谁」——它从来没记过

现在和「人」有关的东西只有三样，全是**当下时点**的：`S.trust`（当前名单对你的信任）、`S.rel`（当前名单互相的关系，而且[没有杠杆](策划稿-更衣室关系.md)）、`S.champCore`（夺冠那一套人的 id 快照，一年就过期）。

退役仪式里那句话也因此只能是通用的：

```
队友：「${S.team}的位置一直给你留着。」    ← cer.ts:491，没有名字
```

作者说的「有的感触」，正是被这一行 `delete` 删掉的东西。

---

## 三、方案

### 甲、地基：共事账本 `S.mates`（必做，本身不改任何数值）

每一个和你同队过的人留一条，**跨转会、跨退役永不删除**：

```ts
S.mates[id] = {
  id, cn, pos,
  firstSi, lastSi,          // 第一次 / 最后一次同队的赛季
  splits,                   // 一起打过几个赛段
  titles: [],               // 一起拿的冠军
  peakTrust,                // 他对你最高的信任
  roles: { si: "被带" | "并肩" | "带人" },   // 每年你在他面前是什么角色（见乙）
  gone: null | "retired" | "sold" | "youLeft"  // 他是怎么离开的
}
```

写入点就三处：`syncTrust()`（新人进名单 → 建档）、`ageWorld()` 的退役分支（标 `retired`）、`syncTrust()` 的删除分支（标 `sold` / `youLeft`，**但不再删账本**）。

**这一条是所有羁绊内容的地基，不做它下面几条都无处落脚。** 它自己不改任何数值，只是不再把已经发生过的事删掉。

### 乙、认出：你在这支队里是哪个角色（必做）

每个赛段结算算一次，全部用**已有**的数据：

```
roleGap = 这个赛段你的场均评分 − 队友场均评分   （boxscore 的 meGap 取赛段均值）
ageGap  = 你的年龄 − 队友平均年龄
```

| 条件 | 角色 | 一句话 |
|---|---|---|
| `roleGap <= −0.15` 且 `ageGap < 0` | **被带** | 「这一年你是队里最年轻、也最靠后的那个。」 |
| `roleGap >= +0.15` 且 `ageGap > 0` | **带人** | 「这一年队伍是你在扛。」 |
| 其余 | **并肩** | 「这一年你们五个人谁也没落下。」 |

写进 `S.mates[id].roles[si]`，也写一条赛段结算的小结。**零数值影响，纯粹是让游戏「看见」自己已经算出来的东西。**

### 丙、四个时刻（内容，这是作者真正要的）

1. **「交给你了」**——角色第一次从「被带」翻到「并肩 / 带人」的那个赛段结算：
   和你共事最久的那位老将说一句。他的 `指挥` 转 1.5 点给你（他确实在退场，你确实在接班）。

2. **「陪他加练」**——已有的更衣室事件 `rookie`（`team.ts:133`）扩成**可重复的行动**，见丁。
   现在它一整个生涯只触发一两次，而作者说的是「一点点成长」——那得是能反复做的事。

3. **「他超过你了」**——**这是作者点名的那个瞬间**：
   一个和你共事 ≥3 个赛段、且 `roles` 里有过「被带（他）」的队友，赛段场均评分**第一次超过你**。
   → 弹一条大事件，不是坏消息：

   > 赛段结算，`XX` 的场均评分 <b>1.34</b>，你 1.29。<br>
   > 三年前他连着几把打崩，一个人坐在训练室里。<br>
   > <b>现在轮到他带你了。</b>

   给一点心态 / 状态（`addBuff("mood")`），因为这在现实里是让人踏实的事，不是打击。

4. **退役仪式点名**——把 `cer.ts:491` 那句通用台词换成真人：
   - **共事最久的那个**（`splits` 最大）说一句
   - **你带过、后来超过你的那个**（有「他超过你了」记录的）说一句
   - 一起拿过冠军的年份直接引用

   一句「`Ming` 说：那年你 19 岁，是我们四个把你护着打完的」比任何数值都值钱，而且**数据全都现成**。

### 丁、交互：队伍栏的「找人聊聊」（1 个行动点）

「队伍」栏每个队友旁边一个入口，**选项按你和他的角色关系变**：

| 对象 | 选项 | 效果 |
|---|---|---|
| **老将**（比你大 ≥3 岁 / 角色=被带） | 请教运营、请教指挥、听他讲当年 | 你 `运营`/`指挥` 小涨、他信任 +、`S.rel` 全队 + |
| **同龄**（并肩） | 双排、复盘、一起吃饭 | 沿用现有 `duo` 那套，但记进账本 |
| **新人**（角色=带人） | 陪他加练、帮他复盘、把资源让给他 | **他的属性 +**（这是「带出来」的实体）、他信任 ++、你 `心态` + |
| **教练** | 问我还差什么、聊阵容、聊续约 | 主要给**信息**——替补线和续约线现在最缺的就是信息 |

每赛段限次（建议每人 1 次 / 赛段，总共不超过 2 次），**并且同时喂 `S.rel`** —— 正好补上[更衣室关系那份稿子](策划稿-更衣室关系.md)里说的「`addRel()` 零调用点」。两件事一起做，成本比分开做低。

### 戊、生涯名片图上加一行（低成本高回报）

名片图刚上线（`v20260909c/f`），是玩家会截图发出去的东西。在「冠军 / 转会 / 段位 / 粉丝」那一排下面加一行：

> **并肩最久** `Ming` · 6 个赛段 · 2 冠
> **你带出来的** `XX` · S15 加入，S17 综评超过你

这一行就是作者说的「感触」，而且它是整份稿子里**最便宜的一块**——数据全在账本里，画布上加两行字。

---

## 四、要动的地方

| 文件 | 改什么 | 甲乙丙丁戊 |
|---|---|---|
| `state.ts` | `S.mates` 存档字段 | 甲 |
| `team.ts` | `syncTrust()` 不再抹掉历史，改为写账本 | 甲 |
| `main.ts` | `ageWorld()` 退役分支标 `gone:"retired"` | 甲 |
| `postmatch.ts` / 赛段结算 | 用 boxscore 的 `meGap` 算赛段角色 | 乙 |
| 新 `bond.ts` | 账本读写、角色判定、四个时刻、聊聊面板 | 甲乙丙丁 |
| `cer.ts` | `farewell` 换成点名版 | 丙 4 |
| `share.ts` | 名片图加两行 | 戊 |
| `test.ts` | 账本跨转会 / 跨退役不丢；角色判定的三档；「他超过你了」只触发一次 | 全部 |

## 五、分批与风险

| 批次 | 内容 | 数值风险 |
|---|---|---|
| **第一批** | 甲 + 乙 + 戊 | **零**——只记录、只判定、只显示，不动任何数值。名片图那一行当场就能感觉到 |
| **第二批** | 丙（四个时刻） | 极低——只有 ①「交给你了」转 1.5 点指挥、③ 给一个心态 buff |
| **第三批** | 丁（聊聊行动） | **中**——会给队友加属性、给你加属性，等于多了一条成长通道。要 240 局批测盯：联赛/世界赛夺冠率、生涯末综评、队友均值 |

第三批还要小心一件事：**「陪新人加练」把队友练强了，等于把游戏变简单**。建议给队友的加成走**他自己的 `ceil`**（`makeRookie` 已经给每个人定了天花板），而且只加在他还没到 `ceil` 的时候——这样「带出来的人」上限还是他自己的，你只是让他更快到达。

## 六、等作者拍板

1. **先做第一批（甲+乙+戊）止痒？** 它零风险，而且名片图上那两行当场就是「感触」。
2. **「他超过你了」这个时刻，要不要给数值？** 我的建议是给一个正向的心态 buff——这是欣慰，不是打击。
3. **丁的「陪新人加练」能不能真的加队友属性？** 这是唯一会动难度的一条。给（更有代入感、但游戏变简单）还是不给（只加信任和关系）？
