# 策划稿 · 出身与身份加成

**状态**：等作者审批，代码未动。
**起因（玩家原话，2026-09-09）**：

> 我觉得身份选项应该有他专属的加成，如果本来就有可以直接写一行小字出来，
> 可以在 20 点天赋点的基础上，再去提高自己的上限或者很差很差的短板。
> 比如主播队友上来就有高段位和一定的粉丝，体校下来的，那就在原体质基础上加个两点，
> 电竞酒店的儿子就多加两点操作，青训下来的就多加两点心态，选身份的同时也是选天赋。

---

## 一、现状盘点：他要的东西，七成已经在了

### 1.1 两条出身路线的差异不小

| | 主播出身 | 青训出身 |
|---|---|---|
| 起始五维（操 / 运 / 心 / 指 / 体） | 59 / 50 / 55 / 51 / 48 | 64 / 60 / 50 / 53 / 58 |
| 起始人气 | 62 | 14 |
| 起始段位修正（`initialRank`） | −2 | +3 |
| 专属加成 | 直播收益 ×1.7 | 训练收益 ×1.2 |

玩家说的「主播上来就有高段位和一定的粉丝」——粉丝确实有（62 对 14），
但**段位反而是青训更高**（+3 对 −2，理由写在代码注释里：「青训有人带，主播时间被直播分走」）。
这一条要么改数，要么改玩家的预期，见第三节。

### 1.2 18 张背景卡全都带数字

玩家举的两个例子**已经在池子里**：

| 卡 | 属性增减 | 其它 |
|---|---|---|
| 体校退下来的 | 体质 +7、操作 −3 | 恢复 ×1.35 |
| 电竞酒店老板的儿子 | 体质 +3、操作 −2 | 资金 280 万 |

跨度从「校队队长 指挥 +7 / 运营 +2 / 操作 −3」到「伤病复出的青训 心态 +9 / 运营 +3 / 体质 −7」，
每张都有明确代价，`origins.ts` 顶上的注释写明「刻意不等值，差异在形状」。

### 1.3 所以真正缺的只有三件事

| # | 问题 | 性质 |
|---|---|---|
| A | 建档页一个数都不显示，只有名字 + 路线标签 + 故事 | 显示 |
| B | `perk` 字段定义了，全代码库从没被渲染过；而且它写的数是错的（写 +60%，代码是 +70%） | 显示 + 数据错 |
| C | 出身和背景**不动天花板**，天花板只由 20 点天赋决定 | 玩法 |

A 和 B 是「本来就有，写一行小字出来」；C 才是玩家提的新东西。

---

## 二、A + B：把已有的加成写出来（建议第一批做，风险最低）

### 2.1 出身卡上加一行数字

现在每张卡是「名字 + 路线标签 + 故事」。加第三行，用和游戏里其它地方一致的写法
（涨绿跌红、`<i class="up">`）：

```
体校退下来的   [青训路线 · 标准难度]
练过八年短跑，膝盖不行了才转电竞。身体底子是这行最稀缺的东西。
体质 +7 · 操作 −3 · 恢复 ×1.35 · 资金 60 万
```

数字直接从 `BACKGROUNDS` 的字段生成，不另写一份文案——**这是关键**：
`origins.ts` 里已经有过一次「注释写 +9~+11、实际早就失真」的教训，
凡是手写的说明迟早和代码对不上。生成的就永远对得上。

### 2.2 把 `perk` 救活，并且改对

- 出身选定后，在起始能力值那一块下面显示一行：`科班底子：训练收益 +20%` / `流量加持：直播收益 +70%`。
- 主播那条从 `+60%` 改成 `+70%`（代码是 `originMul=1.7`，以代码为准）。
- 顺手补上现在没写进 `perk` 的两条：起始人气差、起始段位修正。

### 2.3 自检：不许再对不上

加一条自检，把 `perk` 里写的百分数和代码里的常量对着比：
`ORIGIN.streamer.perk` 里的数必须等于 `streamIncome` 里的 `originMul`，
青训那条必须等于 `gain()` 里的 `bonus`。改了一边忘了另一边，测试直接红。

这条自检的价值和「用语自检」是一样的——这个仓库已经栽过三次「文案和代码分家」。

---

## 三、C：让出身也动天花板（要作者拍板的部分）

### 3.1 现在的分工

- **天赋 20 点** → 决定天花板（`cap(S.talent[d])`）
- **出身 / 年龄 / 背景** → 决定起始属性，不碰天花板

所以玩家的感受是对的：出身再特别，你的上限还是那 20 点说了算，
「选身份」和「选天赋」是两件互不相干的事。

### 3.2 三个方案

#### 方案一 · 出身给天花板一个小幅偏移（推荐）

每条出身路线自带一组天花板修正，幅度小、有正有负：

| | 操作 | 运营 | 心态 | 指挥 | 体质 |
|---|---|---|---|---|---|
| 主播出身 | +2 | −1 | +1 | 0 | −2 |
| 青训出身 | +1 | +2 | −2 | +1 | 0 |

- 走 `capOf()` 里现成的加法（和 `capExp` 同一条路），不新增结构。
- 幅度控制在 ±2：一点天赋点大约值 2~3 点天花板，所以这相当于「白送 ±1 点天赋的形状」，
  **不改变 20 点的总量，只改变形状**。玩家要的「选身份也是选天赋」有了，平衡不会翻车。

#### 方案二 · 背景卡也动天花板

在 `BACKGROUNDS` 里加一个 `cap:{}` 字段，只给少数几张最有性格的卡：
体校（体质 +3）、国服榜一路人王（操作 +3、运营 −2）、伤病复出（体质 −4、心态 +3）。

- 好处：玩家举的例子正是背景卡，这是最贴他原话的做法。
- 风险：18 张卡都要重新配平，而且开局随机抽三张——运气好抽到「天花板 +3」那张会成为事实上的必选。
- **我倾向只做少数几张，并且只在「已经很极端」的卡上加**，让形状更极端而不是更强。

#### 方案三 · 玩家自己在建档时选一个「专精」

出身选定后再选一个专精方向（±2 天花板），完全由玩家决定。

- 好处：可控、直观。
- 坏处：变成第二套天赋系统，和 20 点重复；而且玩家要的是「身份自带」，不是「再选一次」。
- **不推荐。**

### 3.3 推荐

**方案一 + 方案二的收缩版**：出身给 ±2 的天花板形状（全员生效、可预测），
再给三到五张最有性格的背景卡各加一条 ±3 的天花板修正。
方案三不做。

### 3.4 「很差很差的短板」

玩家特别提到这个。方案一的表里主播体质 −2、青训心态 −2 就是短板，
但 ±2 可能不够「很差」。如果作者想要更强的角色感，可以把幅度放到 ±4，
代价是配平要重跑——见验收。

---

## 四、落地顺序

1. **第一批（A + B）**：出身卡显示数字、`perk` 救活并改对、加一条文案与代码一致性自检。
   纯显示，批测必须与 `main` 逐字节一致。
2. **第二批（C 方案一）**：出身的天花板偏移。240 局批测，五个主指标 1σ 内。
3. **第三批（C 方案二收缩版）**：少数背景卡的天花板修正。同样 240 局。

## 五、验收

- 第一批：240 局批测**逐字节一致**（只动显示）
- 第二批 / 第三批：240 局对比 `main`，联赛夺冠、任一冠军、MSI、世界赛、场均冠军五项都在 1σ 内
- 两条出身的**生涯末实力中位数差不超过 1.5 分**——出身要改变形状，不能改变强弱
- 自检：`perk` 文案里的百分数必须等于代码里的常量

## 六、要作者定的三件事

1. **第一批要不要现在就做？** 它是纯显示、零风险，而且直接回应玩家「有的话写一行小字出来」。
2. **天花板偏移的幅度**：±2（保守，形状变了但不改强弱）还是 ±4（玩家说的「很差很差的短板」）？
3. **主播的起始段位**：玩家以为主播段位高，代码里其实是青训更高（+3 对 −2）。
   要改成主播更高（更符合直觉：主播天天打排位），还是保留现状、在卡上把这个数写出来让玩家看见？

---

*2026-09-09 · 破晓*
