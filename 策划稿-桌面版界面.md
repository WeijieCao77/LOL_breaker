# 策划稿 · 桌面版界面：把东西铺开，别让人上下翻页

**状态**：等作者审批。代码零改动，另附一份静态 UI demo。
**起因**：玩家「冷水鱼 616」在群里（2026-09-09）——

> 感觉 pc 界面怪怪的 / 看起来不舒服 / 点起来有点怪
> 感觉可以把所有东西铺在一个平面内 / 这样不用上下翻页

---

## 一、现在的桌面版是什么样

三条量出来的事实：

**1. 整个游戏被锁在 1000px 里。** `theme.css:80`：

```css
.wrap{max-width:1000px;margin:0 auto;padding:0 20px 80px}
```

1920 宽的屏幕上，**两侧各空 460px**，实际用到的只有 52%。

**2. 这 1000px 里还要先扣掉一条竖导航。** `theme.css:748` 的 `@media(min-width:900px)`：

```css
#stage{display:grid;grid-template-columns:96px minmax(0,1fr);gap:0 16px}
```

96px 的竖栏 + 16px 间距，**内容列实际只有约 848px**——和一台竖着的平板差不多宽。

**3. 内容是一条单列，纵向堆。** 「本周」这一页从上到下依次是：

| | 区块 |
|---|---|
| 信息 | 对阵卡、周标题与剩余行动点、托管条、任务卡、本周周记、突破提示、版本卡、体能条、心态提示、风险提示、日常条 |
| 行动 | 五维训练 ×5、打排位、直播、做内容、休息、战队行动 ×4 |
| 出口 | **开打按钮**、快捷键、自动推进、本周对手提示 |
| 附加 | 伤病卡、替补时的抢首发面板 |

**十一条信息 + 十三个按钮 + 一个出口，全部竖着排在一列 848px 里。**
桌面上通常要滚 **两到三屏**才点得到「开打」——而右边一半屏幕自始至终是空的。

玩家说的「不用上下翻页」，指的就是这件事。

## 二、「点起来有点怪」是什么

这套界面是**先按手机做的**，桌面只是把它居中放大：

- `.act`（行动按钮）最小宽 140px、内边距按手指定的，鼠标看过去是一片**稀疏的大方块**，一屏放不下几个
- 标签栏在桌面变成 96px 竖栏，只有一个图标加两个字，**旁边一大片留白**
- 手机上有意义的「一屏一件事」，到了桌面变成「一屏只有一件事」

**这不是配色或者美术问题，是版式没有为鼠标和大屏做过第二版。**

## 三、方案：桌面「工作台」布局

一句话：**宽屏上把「情况」和「行动」并排放，让「开打」永远在视线里。**
所有改动都写在 `min-width` 媒体查询里——**手机版一个像素都不动**。

### 甲、放宽容器（一行）

```css
@media(min-width:1280px){ .wrap{max-width:1400px} }
@media(min-width:1680px){ .wrap{max-width:1560px} }
```

内容列从 848px 涨到约 1250px。**这一条本身就能少滚一屏。**

### 乙、「本周」拆成两列（核心）

`min-width:1180px` 时，本周那张大卡内部分成左右两栏：

| 左栏 · 情况（你需要知道的） | 右栏 · 行动（你要做的） |
|---|---|
| 对阵卡（对手、战力差、版本相性） | 五维训练 ×5 |
| 体能条 / 状态 / 心态提示 / 风险提示 | 打排位 · 直播 · 做内容 · 休息 |
| 任务卡、周记、版本卡、突破提示 | 战队行动 ×4 |
| 托管条、日常条 | 找人聊聊（羁绊，v20260909i 新增） |

左栏是**读**的，右栏是**点**的。这条分界线本来就存在，只是现在被压成了一列。

### 丙、开打条常驻（这才是「不用翻页」）

宽屏上把出口做成**贴在内容区底部的常驻条**：

```
剩余行动点 3        [ 打 夏季赛第 7 周 · vs JDG → ]   [自动推进]
```

行动点用完之前按钮是灰的（和现在一致），**但它永远在屏幕上**。
现在的问题不是按钮难找，是**它在第三屏**。

### 丁、其它页也铺开

「我的 / 队伍 / 世界 / 记录」在 ≥1180px 用已有的 `.two` 双列。
`.two` 这个类**已经写好了**（`theme.css:231`，820px 以下自动塌回一列），只是几乎没被用上。

### 戊、按钮为鼠标收一档

桌面上 `.act` 的最小宽度从 140px 降到 118px、内边距收紧一档，
一行从 5 个变成 7–8 个。**手机上的 140px 不动**——那是手指的尺寸。

## 四、不做什么

- **手机版不动**：所有规则都在 `min-width:1180px` / `1280px` 里
- **不改配色、不改字体、不改任何一个数值**：这是纯版式
- **不引任何库**：站点 CSP 只放行 self 和 data:，而且这是单文件发布

## 五、风险与验证

| 风险 | 怎么挡 |
|---|---|
| 两列切换时卡片高度不齐、右列吊在半空 | 用 `align-items:start` + 左右列各自 `min-width:0`（`#stage` 那套已经踩过这个坑，注释在 `theme.css:751`） |
| 常驻底条盖住内容 | 内容区补等高的 `padding-bottom`，和手机版底栏同一套做法 |
| 1024×768 之类的小桌面被误伤 | 断点定在 1180px，小桌面继续走现在的单列 |
| 界面测试（jsdom）看不出版式 | 加一条几何自检：宽屏下「开打」按钮必须在首屏内 |

## 六、等作者拍板

1. **断点定在 1180px 合适吗？**（1366×768 的老笔记本会走单列；要不要压到 1100px）
2. **常驻开打条**做成贴内容区底部，还是跟着右栏走（滚动时留在右栏顶部）？
3. 先做甲+丙（放宽 + 常驻出口，两条最省事、见效最大），还是甲到戊一次做完？
