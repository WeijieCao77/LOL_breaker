# 破晓 · 玩家三条反馈（颁奖夜排版 / 经济崩了 / 一阵二阵全是不认识的人）策划稿

2026-09-07。**只做策划，不动代码，不部署。**下面每一个数字都是在这个仓库里跑出来的，
诊断脚本在临时目录（跑法记在最后一节），游戏文件一行没改。

玩家原话（群里三条，附截图）：

> ① @点哥，你这个年度二阵为什么在一阵上面，还有为什么都是一个战队的，ming 只有 70 综评了咋上的一阵，
> 不应该是本届赛事各个位置最好的选手上来吗
>
> ②（截图：年薪 12860 万/赛段、签字费 0、合同年限 2 赛段、违约金 5619 万）一个赛段一亿两千万，
> 左手巅峰期只够我零头 —— 四连冠的魅力
>
> ③ 模拟的一阵二阵都是不认识的人，和原本的现实一阵二阵名单差距太大

三条看着是三件事，其实**第一条和第三条同源**：颁奖夜只是那面镜子，
镜子里照出来的是「联赛里已经没有你认识的人了」。第二条是独立的一条指数增长。

---

## 〇、结论先写

| # | 玩家看到的 | 真正的病根 | 位置 |
|---|---|---|---|
| 1a | 二阵排在一阵上面 | 卡片就是按「二阵 → 一阵」写死的；配套的入场动画序号还错位，三个小标题都在自己那几行**之后**才淡入 | `cer.ts:203–206` |
| 1b | 一阵五个人全是一支队；Ming 70 综评也能进 | 评分里加了一项**最高 19 分的团队 bonus**，而同联赛「最强 − 中位」的实力差中位只有 15.1 分 —— 冠军队的五个人被整体抬过所有人 | `cer.ts:91`、`cer.ts:98` |
| 2 | 年薪 12860 万/赛段 | 续约是**乘法**：每次 ×1.05–1.9，**没有任何上限**，一年一次，连乘七次 | `team.ts:251` |
| 3 | 一阵二阵都是不认识的人 | 二队（LDL）85 个青训**全是 18 岁**、实力挤在 56–66，而 18–21 岁每年 **+6.28**、22 岁以上只有 +2.96 —— **两三年后整批越过一队**；提拔通道**没有任何笔数上限**，一整届 LPL 首发被换掉，明星被下放到二级联赛 | `main.ts:1293`、`main.ts:1553`、`market.ts:21` |

三条都不需要改玩法框架，都是**数值与排序**层面的修。

---

## 一、颁奖夜（玩家反馈 ①）

### 1.1 二阵在一阵上面 —— 就是这么写的

`cer.ts` 的 `awardsBody()`：

```
203    <div class="aw-sec" style="--i:0">年度二阵</div><div class="aw-list">${sec}</div>
204    <div class="aw-sec" style="--i:${i}">年度一阵</div><div class="aw-list">${fir}</div>
205    ${rk?`<div class="aw-sec" style="--i:${i}">最佳新秀</div>...`:""}
206    <div class="aw-sec" style="--i:${i}">年度 MVP</div><div class="aw-list">${mv}</div>
```

`.aw-list` 是 `flex-direction:column`（`theme.css:1449`），没有任何 `reverse`，
所以 **DOM 顺序就是屏幕顺序**：二阵在上、一阵在下。玩家截图和代码完全对得上。

写成这样多半是想学颁奖礼「先念二阵、压轴念一阵」的现场感。问题是这张卡**不是逐条揭晓的现场**，
它是一次性铺满的静态榜单 —— 静态榜单没有「压轴」，只有「从高到低」。玩家的直觉是对的。

### 1.2 顺带一个真 bug：入场动画的序号错位

同一段代码里 `i` 是行计数器：

```
196  let i=0;
197  const sec=a.second.map(x=>row(x,i++)).join("");   // 二阵五行用 0–4，i→5
198  const fir=a.first .map(x=>row(x,i++)).join("");   // 一阵五行用 5–9，i→10
199  const rk=...i++...                                //           10，i→11
200  const mv=...i++...                                //           11，i→12
```

到 203–206 行拼模板时 `i` 已经是 **12**，于是「年度一阵」「最佳新秀」「年度 MVP」
三个小标题**全部**拿到 `--i:12`，动画延迟 `12×0.22 = 2.64` 秒。
效果是：一阵那五行在 1.1–2.0 秒之间依次淡入，它们的**标题 2.64 秒才出现**；
后面两个小标题也和它挤在同一帧。「年度二阵」写死 `--i:0` 反而是唯一正确的那个。

这条和排版顺序是同一处代码，一起改。

### 1.3 「都是一个战队的」「Ming 70 综评也能进」—— 团队 bonus 太重

`cer.ts:91` 的评分：

```
91   const bonus=n=>(n===wc?8:0)+(n===mc?4:0)+(n===lc?5:0)+(rk.indexOf(n)<4?2:0);
98   let sc = 五维均值 + (状态−52)/8 + bonus(队名)
```

`wc` / `mc` / `lc` 分别是这一年的世界赛 / MSI / 联赛冠军队。
包揽三冠 + 常规赛前四 ⇒ **bonus = 8+4+5+2 = 19**，而且是**整队五个人每人都加 19**。

跑 8 局全生涯（64 个颁奖夜）量出来的对照：

| 量 | 数值 |
|---|---|
| 同联赛「最强 − 中位」的五维差 | 中位 **15.1** 分 |
| 团队 bonus 上限 | **19** 分 |
| 一阵位置被 bonus 抢走（入选者不是该位置五维最强） | **113/310 = 36%** |
| 本队三冠那一年，一阵里本队人数 | **5/5** |
| 三冠那一年，入选者比该位置最强低多少 | 平均 **16 分**（非三冠年只有 4.6 分） |
| 三冠那一年入选者的最低综评 | **68** |

一个实测样例（S16，本队三冠）：

```
一阵本队 5/5，入选综评 [—, 78, —, 90, 68]
该位置全联赛最强 [87, 78, 88, 90, 84]
```

辅助位：**68 分的人拿了一阵，84 分的人没上榜**。玩家说的「Ming 只有 70 综评」就是这个。
另外顺带印证了玩家的第二句：三冠赛季在 64 个样本里只出现 1 次，
所以这个极端场景平时看不见 —— 但「四连冠」的玩家每年都在这个场景里。

**不是说团队成绩不该算。**玩家自己发的第二张截图（豆包给的 2025 LPL 年度最佳一阵：
上单 Flandre、打野 Tarzan、中单 Knight、AD JackeyLove、辅助 Kael）里，
AL 一家就占了 3 个 —— 冠军队本来就该多占位置。
（那张图顶上写着「AI 生成可能有误」，具体名单落地前请作者自己核一遍；
这里只用它的**结构**：强队占多数、但不是全部。）

问题是量级：**19 分的加成压过了 15 分的实力差**，
于是不是「冠军队多占几个」，而是「冠军队全占」。

### 1.4 另外两处小的

- `cer.ts:108` `const rookie=rows.find(x=>x.age<=20)` —— 「最佳新秀」只看年龄 ≤20，
  不看是不是**新人**。一个 18 岁上一队、打了三年的人 20 岁还能再拿一次最佳新秀。
- `cer.ts:107` `const mvp=rows[0]` —— MVP 用同一份被 bonus 污染的分。

### 1.5 方案

**A. 排版（必做，改动最小）**

改成 `年度一阵 → 年度二阵 → 最佳新秀 → 年度 MVP`，
并把三个小标题的 `--i` 改成各自区块**第一行之前**的序号（标题先、行随后）。
MVP 放最后是对的 —— 它是单独一行的压轴，和「一阵/二阵」这种榜单不是一回事。

> 如果作者仍想保留「压轴揭晓」的仪式感，替代方案是：**保持一阵在上**，
> 但让一阵那五行的动画延迟排在二阵**之后**（视觉上从下往上点亮）。
> 建议先做最简单的 A，玩家提的就是位置。

**B. 团队 bonus 降权（必做）**

| 项 | 现在 | 建议 | 理由 |
|---|---|---|---|
| 世界赛冠军 | +8 | **+3** | |
| MSI 冠军 | +4 | **+1.5** | |
| 联赛冠军 | +5 | **+2** | |
| 常规赛前四 | +2 | **+1** | |
| 合计上限 | **19** | **7.5** | 约等于实力差中位（15.1）的一半 —— 势均力敌时冠军队赢，差一个档次时赢不了 |

**C. 一支队在一阵里最多 3 人（建议）**

按分排完之后，若某队已占满 3 席，该队第 4、5 人顺延到二阵，位置由次名递补。
3 这个数就是照 2025 年真实一阵（AL 3 人）定的，既保住「王朝气质」，又不会出现 5/5。

**D. 最佳新秀要真的是新秀（建议）**

给选手加一个 `debutSi`（第一次进一队的赛季下标），
`rookie` 的条件从 `age<=20` 改成 `debutSi===S.si`（当季首次登场），年龄不再是条件。
老存档没有这个字段的，回落到现在的 `age<=20`。

**E. MVP 从一阵里选（建议）**

MVP 用降权后的分，且限定在一阵五人内取最高 —— 现实里 MVP 不可能不进一阵。

> 更彻底的做法是**把评分换成「赛季表现」**（`S.standings` 里的战绩 + 季后赛走多深 + 逐周状态），
> 而不是「五维快照 + 团队加分」。这是对的方向，但改动面大，
> 建议**这一轮先做 A–E**，把玩家看得见的荒谬修掉，赛季表现口径单开一稿。

---

## 二、经济：年薪一亿两千万（玩家反馈 ②）

### 2.1 病根一行话：续约是没有上限的乘法

`team.ts:251`（`contractCheck()`）：

```
251   const raise = clamp(1.15 + (ovr-teamAvg)*0.02 + (wonTitle?0.15:0), 1.05, 1.9);
255   salary: Math.round(old.salary * raise)
256   buyout: Math.round(old.buyout * raise)
```

合同是 **2 赛段 = 1 年**，所以**一年乘一次**，`clamp` 的上界是 **1.9**，
而且 `raise` 只作用在「上一份合同」上 —— **没有任何绝对上限**。
再叠上谈判里的「要求加薪 年薪 +25%」（`tryout.ts:559`，可以反复提），复利就跑起来了。

跑 8 局「强玩家 + 不转会」（正是玩家那种四连冠留队路线）量出来：

| 赛季 | 年薪中位 | 年薪最高 | 违约金中位 | 违约金最高 |
|---|---|---|---|---|
| S12 | 28 万 | 400 万 | 166 | 2200 |
| S13 | 400 万 | 594 万 | 2200 | 3513 |
| S14 | 472 万 | 989 万 | 2595 | 5849 |
| S15 | 596 万 | 1687 万 | 3299 | 9976 |
| S16 | 775 万 | 2823 万 | 4482 | 16691 |
| S17 | 1130 万 | 4742 万 | 6217 | 28036 |
| S18 | 1473 万 | 7770 万 | 8104 | 45940 |
| S19 | 2113 万 | **12604 万** | 11627 | **74521** |

**S17 的最高值 4742 万，玩家截图是 S17 的 12860 万** —— 他那条线比批测里最贪的一条还要陡
（他还在谈判里加过薪）。这不是异常存档，这是这套公式的正常输出。

### 2.2 三个连带的荒谬，都能在截图里看见

**(a) 尺子全废了。**同一套经济里的其它数字：

| 项 | 金额 |
|---|---|
| 豪门新签年薪区间 | 100–400 万/赛段（`tryout.ts:46`） |
| 世界赛冠军奖金 | 550 万（`shop.ts:58`） |
| LPL 季后赛冠军奖金 | 200 万 |
| 请私教 / 理疗师 / 团建 / 公关 | 60 / 42 / 55 / 35 万 |
| 全套顶级外设 + 全部课程 + 全部资产 | 一次性 **4240 万**（1770 + 780 + 1690） |

一个赛段 12860 万，等于**23 个世界冠军奖金**，等于把全套外设课程资产**买三遍还有富余**。

批测（含再战三年）的生涯累计：

| | 普通玩家 | 强玩家 + 不转会 |
|---|---|---|
| 生涯总收入 | 15297 万 | 19689 万 |
| 其中工资 | 7420 万 | 11908 万 |
| 其中赛事奖金 | 2720 万 | 2690 万 |
| 工资 : 奖金 | **2.7 : 1** | **4.4 : 1** |
| 生涯总支出 | 532 万 | 1049 万 |
| 支出 / 收入 | **3%** | **5%** |

**收进来的钱 95% 以上花不掉。**而且「拿冠军」在钱这条线上已经不重要了 ——
工资是奖金的 3–4 倍，冠军奖金 550 万还不到晚期一个赛段工资的 5%。

**(b) 违约金比年薪还低。**玩家截图：年薪 12860 万，违约金 5619 万。
现实里违约金是年薪的数倍（买断价 = 剩余合同价值 + 溢价），这里反过来了。
原因是两条独立的路径：`buyout` 也在乘 `raise`，但玩家在谈判里用过「压低违约金 −40%」
（`tryout.ts:568`）而「要求加薪 +25%」也用过，两边被拉开了。

**(c) 留队和转会是两套价。**`makeProDeal()`（`tryout.ts:1338`）转会时
**从 `CLUB_TIERS` 的价目表重新算**，完全不看你现在的合同：

```
1338   let salary = Math.round(lerp(T.pay[0], T.pay[1], q));   // 豪门也就 100–400 万
```

也就是说：**留在原队 → 1.28 亿；转会去另一家豪门 → 400 万**。
差 32 倍。这不只是数值失衡，是**同一个游戏里两套互相不认账的定价体系**。

### 2.3 现实锚点

LPL 薪资从来没有官方公布过，能引用的都是转会期的传闻口径，**只用来定天花板的量级，不当事实抄**：
顶薪超巨大致 **2000–4000 万/年**，主力首发多在 **300–1500 万/年**，新秀 **50–200 万/年**。
（这一档数字请作者按自己掌握的信息再校一次 —— 下面的建议值只依赖「顶薪是起薪的几倍」这个比例，
把顶薪整体上调或下调都不影响结构。）游戏里 1 年 = 2 赛段，所以：

| 档 | 建议的赛段薪资上限 | 折合年薪 |
|---|---|---|
| 豪门 | **1500 万/赛段** | 3000 万/年 |
| 中游 | **700 万/赛段** | 1400 万/年 |
| 弱队 | **300 万/赛段** | 600 万/年 |
| 二队/青训 | **40 万/赛段** | 80 万/年 |

顶薪 1500 万/赛段 ≈ 现在豪门起薪上限（400 万）的 3.75 倍 ——
「打了七年、拿了四个冠军，工资涨到出道时的三倍多」，这是玩家能接受也能理解的曲线。

### 2.4 方案

**A.（推荐）把「续约」和「转会」并到同一个身价函数上**

新增一个 `marketValue()`：读**实力（五维）、荣誉（联赛/MSI/世界赛冠军数）、人气、年龄、俱乐部档次**，
输出这个人此刻在这个档次俱乐部的「市场价」。然后：

- 续约报价 = `clamp(marketValue(), 上一份合同 × 0.85, 该档次上限)`
  —— 老合同只当**地板**（防止砍薪羞辱），不再当**乘数基数**（复利从这里断掉）
- 转会报价 = 同一个 `marketValue()`，只是俱乐部档次换成买方的
  —— 留队和转会终于是同一把尺
- 谈判的 +25% 仍然可以提，但结果照 `该档次上限` 截断

一处改动同时修掉 2.1（指数）、2.2(c)（两套价）。

**B.（保底，改动最小）给乘法加天花板**

如果不想动结构，最小的修法是在 `team.ts:251` 之后加一行：

```
salary = Math.min(salary, PAY_CAP[clubTier])     // 豪门 1500 / 中游 700 / 弱队 300 / 青训 40
```

并把 `raise` 的上界从 **1.9 收到 1.35**（`clamp(..., 1.02, 1.35)`）——
越接近上限涨得越慢，符合直觉。这条一行就能落地，但不修「转会掉价 32 倍」。

**C. 违约金改成年薪的倍数（配套）**

`buyout` 不再独立乘 `raise`，改成 `salary × k`，`k` 按档次 3–6，
谈判里的「压低违约金 −40%」改成调 `k`（6 → 3.6）。这样违约金永远高于年薪，
「买断价 < 年薪」的荒谬消失。

**D. 文案（顺手）**

- `年薪 12860 万/赛段` 本身就是病句 —— 「年薪」配「/赛段」。
  建议改成 **「赛段薪资 X 万（年薪 2X 万）」**，两个口径一次说清。
- 数字过万（≥10000 万）时显示成 **「1.29 亿」**，别让玩家自己数零。
  即使修完数值不该再出现亿级，显示层也该兜住。

**E. 钱要有地方去（建议，可以下一稿）**

支出只占收入 3–5% 是另一个独立问题。方向：给冠军级收入配冠军级的去处
（家人、青训基金、个人品牌、俱乐部股份……）。不在这一轮范围内，先记一笔。

---

## 三、一阵二阵全是不认识的人（玩家反馈 ③）

这条最严重，因为它**不是颁奖夜的问题** —— 是到了第五年，LPL 首发席上真的已经没几个真人了。

### 3.1 数据：真名是怎么消失的

跑 8 局，每个休赛期数一遍 LPL 一队 85 个首发位（不含主角）：

| 赛季 | 2022 真名占比 | 二队（LDL）名字占比 | 生成新秀占比 | 平均年龄 |
|---|---|---|---|---|
| S12 | **0.99** | 0.00 | 0.05 | 22.0 |
| S13 | 0.93 | 0.06 | 0.05 | 22.6 |
| S14 | 0.69 | 0.28 | 0.06 | 22.4 |
| S15 | 0.40 | 0.57 | 0.06 | 22.2 |
| S16 | **0.25** | **0.72** | 0.06 | 22.6 |
| S17 | 0.16 | 0.80 | 0.06 | 23.3 |
| S18 | 0.12 | 0.81 | 0.10 | 23.9 |
| S19 | 0.09 | 0.76 | 0.17 | 24.4 |

**注意平均年龄一直是 22–24 岁 —— 联赛没有老化，真名是被「换」掉的，不是「退役」掉的。**

拆开每一次换人的来源（6 局，累计 579 人次）：

| 来源 | 人次 | 占比 |
|---|---|---|
| **二队提拔** | **544** | **94%** |
| 生成新秀（顶替退役） | 34 | 6% |
| 别队真人（队间转会） | 1 | 0.2% |

一局生涯平均换 **97 人次**，而 LPL 一队总共只有 **85 个位置**。
**整届 LPL 首发被换了一遍还多。**其中 94% 走的是同一条通道：二队提拔。

按赛季看提拔笔数（每局平均）：S13 5.2 → **S14 23.0 → S15 24.8** → S16 13.0 → S17 9.8 → S18 4.7 → S19 10.2。
S14–S15 是雪崩点。

### 3.2 更糟的：明星没退役，他们在二队坐着

数 `STARS`（`stars.ts`，41 位有履历的明星）此刻在哪：

| 赛季 | 在一队 | **在二队（LDL）** | 被下放的样例 |
|---|---|---|---|
| S12 | 17.0 | 0.0 | — |
| S14 | 14.3 | 1.7 | Doinb Bin Crisp Uzi |
| S15 | 12.8 | 3.2 | Yagao Crisp Doinb Scout Bin Uzi Tian |
| S16 | 9.0 | 5.7 | Tian Yagao TheShy Rookie Crisp Scout Bin Jiejie |
| **S17** | **5.7** | **8.7** | Uzi Ming Tian Jiejie Viper Yagao TheShy Rookie |
| S19 | 3.5 | 5.2 | Tian Knight Jiejie Viper Kanavi Rookie Bin 369 |

**从 S17 起，「明星」在二级联赛的人比在 LPL 的还多。**
TheShy、Rookie、Uzi、Viper、Kanavi、369、Knight —— 全在 LDL 打次级联赛。
他们既没退役（`stars.ts` 的聚光灯还认得他们），也没转会，就是被自家青训顶下去了。

这才是玩家说的「差距太大」的真正来源：不是名单被随机数打乱了，
是**联赛把自己的招牌全下放到了二队**。

### 3.3 为什么必然发生：三条叠在一起

**(a) 85 个青训一律 18 岁，实力挤在 56–66 的窄带里** —— `main.ts:1293`（`buildLDL`）：

```
1293   makeRookie(x.k, Math.max(56, Math.min(p-8, lplLow)), "LDL")
```

`makeRookie`（`main.ts:1336`）写死 `age:18` —— **85 个人全是 18 岁，一个不差**。

实力那头，实测（seed 42，S12 开局）：

| | LPL 一队均值 | 二队生成档位 |
|---|---|---|
| RNG / TES / EDG / JDG | 80.4 / 79.4 / 78.3 / 78.2 | **全部 64.4**（被 `lplLow` 截平） |
| WBG / V5 | 74.4 / 72.9 | 64.4 |
| TT / BLG / OMG / FPX / LNG / IG | 69.9 → 66.6 | 61.9 → 58.6（走 `母队−8`） |
| RA / LGD / AL / UP / WE | 65.2 → 64.0 | 57.2 → 56.0（`max(56,…)` 只在 WE 生效） |

`lplLow`（垫底三队均值）实测 **64.4**，所以真正生效的是「不高于垫底三队」这条帽子，
`max(56,…)` 那个下限几乎从不触发（只有 WE 一家）。
结果是：**LPL 一队铺开在 64–80 的 16 分区间，17 支二队全被压进 56–66 的 10 分窄带**。
豪门青训和弱队青训只差 8 分，而它们的母队差 16 分 —— 豪门青训被压低，弱队青训被抬高。

**(b) 年龄曲线让 18 岁的人每年 +6.3，24 岁的人几乎不动、26 岁起往下掉** —— `main.ts:1553` + `ageCurve`：

```
dev  = age<=23 ? 2.3 : age<=25 ? 1.3 : age<=27 ? 0.4 : 0
base = ageCurve(age)：≤21 +3.6 · ≤23 +0.6 · ≤25 −1.6 · ≤27 −3.6 · 28+ −6.2
每维漂移 = base*(base>0 ? 1.4-DECAY_W*0.5 : DECAY_W) + dev
```

逐年涨幅（五维均值）跑出来是一条**断崖**：

| 年龄 | 18–21 | 22–23 | 24–25 | 26–27 | 28+ |
|---|---|---|---|---|---|
| 每年 | **+6.28** | +2.96 | +0.36 | −1.72 | −3.66 |

把上面那些真实起点代进去（五维同值近似）：

| 赛季 | 豪门青训 64.4/18岁 | 弱队青训 56/18岁 | RNG 首发 80.4/22岁 | 中游首发 68/23岁 | 老将 74/25岁 |
|---|---|---|---|---|---|
| S12 | 64.4 (18) | 56.0 (18) | 80.4 (22) | 68.0 (23) | 74.0 (25) |
| S13 | 70.7 (19) | 62.3 (19) | 83.4 (23) | 68.4 (24) | 72.3 (26) |
| **S14** | **77.0 (20)** | 68.6 (20) | 83.7 (24) | **68.7 (25)** | 70.6 (27) |
| **S15** | **83.2 (21)** | **74.8 (21)** | 84.1 (25) | 67.0 (26) | 66.9 (28) |
| **S16** | **86.2 (22)** | 77.8 (22) | **82.4 (26)** | 65.3 (27) | 63.2 (29) |
| S17 | 89.2 (23) | 80.8 (23) | 80.6 (27) | 61.6 (28) | 59.6 (30) |
| S19 | 89.9 (25) | 81.5 (25) | 73.3 (29) | 54.3 (30) | 52.3 (32) |

- **S14**：豪门青训（77.0）越过中游首发（68.7）—— 实测提拔从 5.2 笔跳到 **23.0 笔**，就在这一年。
- **S15**：连弱队青训（74.8）都越过中游首发 —— 实测 **24.8 笔**，雪崩。
- **S16**：豪门青训（86.2）越过 RNG 那种顶级首发（82.4）—— 最后一批真名也守不住了。
- 到 S19，青训出身的人 **89.9**，当年的联赛招牌 **73.3**，中游首发 **54.3**。

18 岁那四年每年 **+6.28**，22 岁的首发只有 **+2.96**，24 岁起基本停住，26 岁起往下掉。
这不是概率问题，是**算术上必然**：给 85 个 18 岁的人一个 56–66 的起点，
两到三年后他们**一定**整体越过 64–80 的一队 —— 提拔门槛写多少都拦不住，只是早晚。

**(c) 提拔通道没有任何闸门** —— `market.ts:21` / `market.ts:48`：

```
21   export const MKT={promoGap:2, promoP:0.7, xferP:0.35, maxPerLeague:0.3, ...}
48   if(mktOvr(a) < mktOvr(f)+MKT.promoGap || rnd()>=MKT.promoP) return;
```

- 门槛只有 **2 分**（同联赛实力差中位 15 分的 1/7）
- 概率 **0.7**，一年两个窗口（季中 + 休赛期）
- **`maxPerLeague` 只管跨队转会，不管提拔** —— `aiPromotions()` 里没有任何笔数上限，
  17 家 × 5 个位置，符合条件的**一次全换**
- 被顶下去的人**换到二队**，从此再没有回来的路（下个窗口他比新来的弱，永远不会被提回来）

所以每个位置换过一次就锁死。85 个位置 × 一次 ≈ 85 人次，实测 97 人次，对上了。

### 3.4 方案

四条按重要性排，**(a) 和 (c) 是必做，改完就够**。

**A. 青训不能是「85 个 18 岁的同龄人」（必做，这条是根）**

- **年龄铺开**：`buildLDL` 生成时年龄取 **17–21 随机**，而不是全体 18 岁。
  一支二队里有 21 岁快毕业的、也有 17 岁刚进来的，才像真的青训队 ——
  更要紧的是：**同龄人整批冲上 +6.28 的成长台阶，就会整批同时越线**，
  年龄一散开，越线就变成零星事件而不是雪崩。
- **成长曲线要有个人上限**：`makeRookie` 时抽一个 `ceil`（比如起始值 +12 ~ +26），
  逐年成长只在 `ovr < ceil` 时给 `dev`。现在 `dev` 只看年龄不看天花板，
  于是**每一个**青训都能一路涨到 89 —— 现实里绝大多数青训打不上一队就退了。
  这条同时修掉「S19 青训出身的人 89.9 分」这个更大的问题。
- **实力跟着母队铺开**：`Math.min(母队-8, lplLow)` 这顶帽子把豪门青训（应该 72）压到了 64.4，
  和弱队青训（56）只差 8 分。建议把帽子从「垫底三队均值」放宽到「联赛中位」，
  同时把地板从 56 降到 48，让二队的实力带（48–72）和一队（64–80）**有重叠但不齐平**。
- **一支二队只出一个好苗子**：五个人里随机一个给 `+6~10` 的「天才」加成，其余压低。
  现在五个人一样强，所以一提就提五个。

**B. 老将不该整队掉进二队（必做，和 A 同等重要）**

`aiPromotions` 里被顶掉的人现在直接 `f.lg="LDL"` 塞进二队。改成三选一：

1. **转会**：如果联赛里有队伍该位置比他弱，走 `aiTransfers` 的通道去那支队（现实里就是这样）
2. **退役**：`ovr` 已经掉到退役线附近的，直接走退役流程（有事件、进 `retiredPool`、可复出）
3. **下放**：只有前两条都不成立时才下放二队

`STARS` 名单里的人额外保护：**只要还没到退役线，就不进二队**，宁可转会去弱队。
理由很直白 —— 现实里 TheShy 打不动了会转会、会退役，不会去打 LDL。

**C. 提拔要有闸门（必做，一行）**

`aiPromotions` 补上和 `aiTransfers` 一样的限额：

| 项 | 现在 | 建议 |
|---|---|---|
| `promoGap`（青训要强多少才提） | 2 分 | **6 分** |
| 每个窗口全联赛提拔笔数上限 | 无 | **≤ 3 笔** |
| 每支队每年提拔上限 | 无 | **≤ 1 人** |
| `promoP` | 0.7 | 0.7（不动） |

按这套算，一局生涯（16 个窗口）最多约 48 笔，实际远低于此，
且被 B 拦掉一部分 —— 目标是把「一局换 97 人次」压到 **20–30 人次**，
让 S16 的真名占比从 **0.25 回到 0.55–0.65**。

**D. 验收标准（改完拿这几条批测）**

| 指标 | 现在 | 目标 |
|---|---|---|
| S16 LPL 一队 2022 真名占比 | 0.25 | **≥ 0.55** |
| S19 LPL 一队 2022 真名占比 | 0.09 | **≥ 0.30** |
| 一局生涯 LPL 换人总数 | 97 人次 | **20–35 人次** |
| S17 明星在二队的人数 | 8.7 | **≤ 2** |
| LPL 一队平均实力（不能因此停滞） | S12 70.3 → S19 75.7 | **保持逐年上行**，浮动 ±1.5 以内 |

最后一条是防回退的护栏：`main.ts:1546` 那段注释记着上一次的教训 ——
「联赛五年纹丝不动（S12 46.9 → S16 47.1），于是玩家生涯末联赛冠军率 100%」。
这一轮是要**关小水龙头，不是关掉** —— 改完必须重新跑 `--batch 40` 确认夺冠率没有被顶回去。

---

## 四、改动清单（供排期，本轮不落地）

| # | 文件 | 改什么 | 规模 | 风险 |
|---|---|---|---|---|
| 1 | `cer.ts:203–206` | 一阵移到二阵之上；三个小标题的 `--i` 修正 | 4 行 | 无 |
| 2 | `cer.ts:91` | 团队 bonus 19 → 7.5 | 1 行 | 低（要跑批测看一阵成分） |
| 3 | `cer.ts:106` | 一阵单队限 3 人 | ~6 行 | 低 |
| 4 | `cer.ts:107–108` | MVP 限一阵内；新秀改判 `debutSi` | ~8 行 + 一个字段 | 低（老存档回落） |
| 5 | `team.ts:251` + `tryout.ts:1338` | `marketValue()` 统一续约与转会定价 | ~40 行，新函数 | **中**（要重跑经济批测） |
| 5' | `team.ts:251` | 保底方案：加 `PAY_CAP` + `raise` 收到 1.35 | 2 行 | 低 |
| 6 | `tryout.ts` | `buyout = salary × k` | ~6 行 | 低 |
| 7 | `shop.ts` / `tryout.ts` | 「赛段薪资（年薪 2X）」文案 + 万→亿显示 | ~10 行 | 无 |
| 8 | `main.ts:1293` `main.ts:1336` | 青训年龄铺开 17–21、实力带放宽到 48–72、一队一个苗子 | ~12 行 | **中** |
| 8' | `main.ts:1336` `main.ts:1553` | 给每个 AI 选手一个成长天花板 `ceil`，`dev` 只在 `ovr<ceil` 时给 | ~6 行 | **中**（会压低全联赛水位，要跑批测） |
| 9 | `market.ts:39–58` | 提拔限额（`promoGap` 6、全联赛 ≤3、每队 ≤1） | ~10 行 | **中** |
| 10 | `market.ts:39–58` | 被顶掉的人优先转会 / 退役；`STARS` 不进二队 | ~25 行 | **中** |

建议分三个提交：**① 颁奖夜（1–4）**、**② 经济（5'–7，先走保底方案）**、**③ 名单（8–10）**。
③ 改完必须跑 `npm test` + `--batch 40` 对比夺冠率，它最容易把难度顶回去。

---

## 五、诊断脚本怎么跑

脚本写在临时目录，没有进仓库（游戏文件一行没动）。都是 `.mts`，靠 `demo/test.ts` 的
`playOne({hook})` 钩子在每一步读 `S`，只读不写：

```bash
npm ci
npx tsx <脚本> <局数> [--strong] [--loyal]
```

| 脚本 | 干什么 | 对应本文 |
|---|---|---|
| `diag.mts` | 薪资/违约金逐赛季轨迹、颁奖夜名单成分、LPL 真名占比 | 2.1、3.1 |
| `diag2.mts` | 团队 bonus vs 实力差、一阵位置被抢走的比例 | 1.3 |
| `diag3.mts` | 本队三冠时的一阵扫榜强度 | 1.3 |
| `diag5.mts` | LPL 换人来源拆分（二队提拔 / 退役 / 转会） | 3.1 |
| `diag7.mts` | 生涯累计收支（在 `ledgerRotate` 前累加 `cur`） | 2.2 |
| `diag8.mts` | `STARS` 此刻在一队还是二队 | 3.2 |
| `curve.mts` | 把 `ageCurve + dev` 逐年跑出来看交叉点 | 3.3 |

要重跑的话按上表重写即可，核心只有一句：

```ts
import { playOne, A } from "/绝对路径/demo/test.ts";
playOne({ seed: 1234, strong: true, loyal: true, encore: true, hook: (S, A) => { /* 读 S */ } });
```

注意两点：① 脚本必须是 `.mts` 或放在有 `"type":"module"` 的目录下，
否则 tsx 会按 CJS 编译，`demo/test.ts` 的顶层 await 会报错；
② `demo/src/achieve.ts` 有一处循环引用，偶尔会抛
`Cannot access 'ACH_MORE' before initialization` —— 重跑一次就好，不影响结果。
