# 破晓 · 首发竞争的刻度 与 续约的判据 策划稿

2026-09-08。**只做策划，不动代码。**下面的数字都是在当前 `main` 上跑批测量出来的。

玩家（爱发电，咬一口肥肥的鱼）原话：

> 首发竞争数值有点糟糕，实力和队内成员差不多，就低一两点的时候老是只有二三十，不给续约。
> 哪怕实力是院长级别也就五六十。而且什么队魂和教练信任这些对续约毫无影响，
> 只要首发竞争数值一低，哪怕教练信任高，队魂随便就不续约丢出去，观感有点差。

**结论：三条全部属实，而且比他说的还多一条。**

---

## 〇、四条一次说完

| # | 玩家看到的 | 真正的原因 | 位置 |
|---|---|---|---|
| 1 | 「低一两点却只有二三十」 | 竞争条**每差 1 分动 6 点**，而且它用的是**裸五维**，你在「我的」页看到的实力**含外设**（满配 +2.9）。你眼里的「低一两点」在它眼里是低四五分 | `press.ts:148` |
| 2 | 「院长级别也就五六十」 | 竞争条是**纯相对量**，只看你和队友的差。强队里的世界级选手也就比队友高 1 分 → 61 | 同上 |
| 3 | 「教练信任高、队魂也照丢」 | 续约判据里**根本没有教练信任、经理信任、在队时长**，只有「实力差 ≥ −6 且队友信任 ≥ 35」的硬 AND | `team.ts:284` |
| 4 | （玩家没提，但更糟） | 界面上明写着「经理信任 …续约和转会的桌上，这个数字都在」—— **这句话是假的** | `press.ts:161` |

---

## 一、首发竞争：刻度太陡，而且用错了尺

### 1.1 现在的算法

```js
// press.ts:148（rotation.ts:328 是同一把尺的副本）
const me   = avg(DIMS.map(d => S.attrs[d]));          // 裸五维，不含外设
const tavg = 队友五维均值;
comp = clamp(55 + (me - tavg) * 6, 5, 98);
```

**每差 1 分，条动 6 点。**映射表：

| 你比队友 | −6 | −4 | −2 | **−1** | 0 | +1 | +2 | +4 | +6 |
|---|---|---|---|---|---|---|---|---|---|
| 竞争条 | 19 | 31 | 43 | **49** | 55 | 61 | 67 | 79 | 91 |

### 1.2 「低一两点却只有二三十」是怎么出来的

光看上表，低 1–2 分是 43–49，对不上玩家说的二三十。**差的那一截是外设。**

- 「我的」页显示的实力走 `myStrength()` = 五维 + 外设加成（`main.ts:1466`）
- 竞争条走 `avg(S.attrs)` —— **不含外设**
- 满配顶级外设的加成：操作 +5.5 · 运营 +5.7 · 体质 +3.3 · 心态 0 · 指挥 0 → **均值 +2.9**

所以一个买满外设的玩家，**界面告诉他「和队友差不多」的时候，竞争条眼里他低了 2.9 分**；
他觉得自己「低一两点」时，竞争条算的是低 3.9–4.9 分 → **条上就是 25–31**。

**和玩家报的数字严丝合缝。**这不是他感觉错了，是两个地方用了两把尺。

（批测里这个差是 0，因为机器人不买外设——所以这条 bug 批测永远抓不到，只有真玩家会撞上。）

### 1.3 红字出现得太频繁

`comp < 45` 时界面显示红色 + 「你是队里最薄的一环——教练在看替补名单」。
批测 4649 个赛季周的分布：

| 竞争条 | 0–25 | 25–45 | 45–60 | 60–75 | 75–99 |
|---|---|---|---|---|---|
| 占比 | 16% | 19% | 21% | 18% | 27% |

**35% 的时间在红区。**一个正常打的玩家有三分之一的赛季周被告知「你随时会被换掉」——
这不是紧张感，是噪音。

### 1.4 「院长级别也就五六十」：纯相对量的必然结果

竞争条只看队内差。在一支强队里：

- 你是世界级，队友也是世界级 → 差 +1 → **条 61**
- 你在垫底队当核心，队友很菜 → 差 +6 → **条 91**

于是「在 RNG 当世界级中单」显示 61，「在垫底队当矮子里的将军」显示 91。
**这条尺量的是「你在队里排第几」，但玩家读它的时候当成了「我这个首发稳不稳」。**
这两件事在强队里恰恰相反——越强的队，你越难显得突出，但你的位置其实越稳。

---

## 二、续约：教练信任、经理信任、队魂，一个都没进式子

### 2.1 现在的判据

```js
// team.ts:284
const trust = avgTrust();                                   // 队友信任均值
const wantRenew = wonTitle || (ovr >= teamAvg - 6 && trust >= 35);
```

三件事玩家以为有用，实际上都没进这个式子：

| 指标 | 实际用在哪 | 对「给不给续约」的影响 |
|---|---|---|
| **教练信任** `coachTrust()` | `tryout.ts:1119` 的谈判底气 —— 决定**已经有报价之后**你能谈多少 | **零** |
| **经理信任** `mgrTrust()` | `clout.ts` 的转会问询范围 | **零** |
| **在队时长**（队魂）`teamTenure()` | 只用来解锁几个更衣室/际遇事件 | **零** |

而且它是一个**硬 AND**：实力差一旦低于 −6，冠军之外没有任何东西能救。

### 2.2 实测：被丢出去的恰恰是待得最久的人

24 局（普通天赋），90 次合同到期：

| | 实力差 | 教练信任 | 经理信任 | 队友信任 | 在队场次 |
|---|---|---|---|---|---|
| 续约（85 次） | +3.6 | 40.7 | 65.2 | 84.3 | 59.2 |
| **放走（5 次）** | **−9.9** | **51.7** | **70.7** | **83.7** | **68.2** |

**被放走的人，教练信任更高、经理信任更高、在队时间更长。**
5 次里有 4 次（80%）是在队满 4 赛段以上的老人。

强玩家那一批里那唯一一次被放走更典型：**教练信任 62.9、经理信任 76.1、队友信任 83.7、
在队 61 场 —— 全都很高，仅仅因为实力差 −9.1 就走人。**

这就是玩家说的「队魂随便就不续约丢出去」。他没有夸张。

### 2.3 界面在骗玩家

`press.ts:161`：

```js
${bar("经理信任", mt, mt<35?"var(--red)":null, mt<35?"续约和转会的桌上，这个数字都在":null)}
```

经理信任低于 35 时，界面**明确告诉玩家「续约和转会的桌上，这个数字都在」**。
转会那半句是真的（`clout.ts:192` 确实用了），**续约那半句是假的**。

玩家照着这句话去养经理信任，养到 76 照样被放走——观感差就是这么来的。

### 2.4 附带发现：`teamTenure()` 的单位是「场次」不是「赛段」

```js
// main.ts:2448
export function teamTenure(){ return Math.max(0, 总场次 - S.teamSince); }
```

它返回的是**比赛场数**。而更衣室事件里的门槛写的是：

```js
when:()=>(teamTenure())>=3 && ...   // team.ts:133「新秀」
when:()=>(teamTenure())>=4 && ...   // team.ts:143「老将」
```

写这两条时的本意多半是「在队 3–4 个**赛段**」，实际上**打 3–4 场就满足了**。
所以「老将私下问你是不是该退了」这种事，你加盟第一个月就会遇到。
这条和续约无关，但既然要动 `teamTenure`，一起收拾。

---

## 三、要改成什么样

三个目标，按重要性排：

1. **两把尺合成一把** —— 界面上说的实力和判据里用的实力必须是同一个数
2. **续约要看得见的东西真的算数** —— 教练信任、经理信任、队魂进式子，但救不了太大的实力差
3. **竞争条要能反映绝对实力** —— 不能只告诉你「队里排第几」

### 3.1 竞争条：统一口径 + 放缓刻度 + 加入联赛维度

```js
const me   = myStrength();                    // 改：和「我的」页、队伍页同一把尺（含外设）
const tavg = 队友五维均值;
const lavg = 本联赛同位置的实力均值;            // 新：绝对坐标
comp = clamp(55 + (me - tavg) * 4 + (me - lavg) * 2, 5, 98);
```

三处改动，各解决一条：

| 改动 | 解决 |
|---|---|
| `me` 改用 `myStrength()` | 1.2 的口径差（玩家看到的和判据用的终于是一个数） |
| 斜率 6 → **4** | 1.1 的悬崖感 |
| 加入 `(me − lavg) * 2` | 1.4 的「院长级别只有五六十」 |

新的映射（假设你在联赛里处于中游）：

| 你比队友 | −6 | −4 | −2 | −1 | 0 | +1 | +2 | +4 |
|---|---|---|---|---|---|---|---|---|
| 旧 | 19 | 31 | 43 | 49 | 55 | 61 | 67 | 79 |
| **新** | **31** | **39** | **47** | **51** | **55** | **59** | **63** | **71** |

而「强队里的世界级选手」：队内 +1（+4）、联赛同位置 +8（+16）→ **75**，
不再是让人误会的 61。「垫底队里的矮子将军」：队内 +6（+24）、联赛 −10（−20）→ **59**，
比现在的 91 诚实得多。

配套：
- 红区阈值 **45 → 35**（对应比队友低约 5 分），文案从「你是队里最薄的一环」
  改成分档说法：≥70「队伍围绕你打」/ ≥50「位置稳固」/ ≥35「替补在追你」/ <35「教练在看替补名单」
- `rotation.ts:328` 那份副本跟着改（它现在是复制粘贴的同一行，两边必须同步——
  **建议抽成一个 `starterComp()` 导出函数，两边都调它**，避免以后又各改各的）

### 3.2 续约：从硬 AND 改成加权评分

现在是「过线就留、不过线就走」，建议改成一个**留队意愿分**：

```js
留队意愿 = (实力差 + 6) * 8            // 实力仍是主项：差 −6 时贡献 0
         + (教练信任 − 50) * 0.35      // 教练：满信任约 +17
         + (经理信任 − 50) * 0.25      // 经理：满信任约 +12（界面那句话终于成真）
         + (队友信任 − 50) * 0.20      // 队友：满信任约 +10
         + min(在队赛段, 6) * 2.5       // 队魂：待满三年约 +15
         + 今年冠军 ? 60 : 0            // 冠军仍然是铁续约
         + 年度一阵 8 / 二阵 4 / MVP 12
留队 = 留队意愿 >= 30
```

**这套的性质：**

| 情形 | 现在 | 改后 |
|---|---|---|
| 实力差 −3、教练信任 75、在队 4 赛段 | 续约（勉强） | 续约（很稳） |
| **实力差 −8、教练信任 75、经理 70、在队 5 赛段** | **放走** | **留下**（这就是玩家说的队魂） |
| 实力差 −8、教练信任 35、在队 1 赛段 | 放走 | 放走 |
| **实力差 −14、教练信任 90、在队 6 赛段** | 放走 | **仍然放走**（信任救不回崩掉的实力） |

也就是说：**信任和队魂大约能抵掉 4–5 分的实力差**，这个量级正好是
「他确实退步了，但俱乐部还想再给他一年」——现实里的老将续约就是这么谈的。
再多就救不动，这条线要守住，不然续约变成永远不会失败的事。

### 3.3 把隐性规则显性化

改完之后，界面要把账算给玩家看。建议在「队内身份」卡下面加一行：

> 续约桌上：实力差 −8 · 教练信任 75（+9）· 经理信任 70（+5）· 队魂 4 赛段（+10）
> → 留队意愿 **34 / 30**，他们还想留你

这一条比数值改动本身更重要 —— 玩家抱怨的是「观感」，
而观感的根源是**他做的努力（养信任、当队魂）看不到回报**。
把这笔账摊开，即使数值一点不动，观感也会好一半。

### 3.4 顺带：`teamTenure` 的单位

- 新增 `teamTenureSplits()`（在队几个赛段），续约评分和更衣室事件门槛都改用它
- `teamTenure()`（场次）保留给确实要数场次的地方

---

## 四、验收标准

| 指标 | 现在 | 目标 |
|---|---|---|
| 竞争条 <45（红区）的赛季周占比 | 35% | **≤15%** |
| 「我的」页实力 与 竞争条口径差 | 满配外设差 2.9 分（条差 17 点） | **0** |
| 被放走的人里「教练信任 ≥60 且在队 ≥4 赛段」 | 80% | **≤10%** |
| 续约率 | 85/90 = 94% | **90%–97%**（不能因此变成 100%） |
| 强队里世界级选手的竞争条 | ~61 | **70–80** |
| 40 局批测其余数字（夺冠率、上岸率） | — | **变化在噪声内** |

最后一条是护栏：这是观感修复，不是难度调整。续约率如果冲到 100%，
就等于把「被裁」这条叙事线砍了——那是另一个问题。

---

## 五、改动清单

| # | 内容 | 文件 | 规模 | 风险 |
|---|---|---|---|---|
| 1 | 抽出 `starterComp()`，`press.ts` / `rotation.ts` 共用 | `press.ts` `rotation.ts` | ~15 行 | 低 |
| 2 | 口径改 `myStrength()`、斜率 6→4、加联赛同位置项 | `press.ts` | ~8 行 | 低 |
| 3 | 红区阈值 45→35，文案分四档 | `press.ts` | ~6 行 | 无 |
| 4 | 续约改加权评分 `renewScore()` | `team.ts` | ~30 行 | **中**（要跑批测看续约率） |
| 5 | 队内身份卡加「续约桌上」明细 | `press.ts` | ~12 行 | 低 |
| 6 | `teamTenureSplits()`，更衣室事件门槛改用它 | `main.ts` `team.ts` `random.ts` | ~10 行 | 低（会让那几个事件晚一些触发） |

建议分两批：**① 竞争条（1–3、5）**，纯显示层，风险低、见效快；
**② 续约判据（4、6）**，动了留/走的分布，要跑 40 局批测对续约率和夺冠率。

---

## 六、一个要作者拍板的点

3.2 里「信任和队魂能抵多少实力差」是这套设计唯一的自由参数。我给的是 **4–5 分**：

- 给少了（2 分）：玩家还是会觉得白养
- 给多了（8 分以上）：只要在一支队待够久就永远不会被裁，「被裁」这条线就死了

4–5 分对应「他确实不如从前，但俱乐部还想再赌一年」。要不要再松一点，你定。
