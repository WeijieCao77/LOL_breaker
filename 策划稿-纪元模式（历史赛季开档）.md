# 破晓 · 纪元模式（历史赛季开档）策划稿

2026-09-08 记录。**这一稿只是把思路存档，不排期、不动代码。**
玩家提出、作者当场认可并说「先做 S7–S9 拿首冠这一块」，所以先把它写下来，免得散在聊天记录里。

---

## 一、原始提议

玩家 **糕喻良**：

> 做纪元模式
> S3-S6 一个纪元（魔王纪元）
> S7-S9 一个纪元（暗夜与双冠）
> S10-S12 一个纪元
> S13-S15 一个纪元
> 可自主选择开档时间

作者回复：

> 可以的，但是得一个个做可能上线的比较慢，还得去校对一下数值因为得去爬这些时期的选手数据和比赛数据，
> 但是我记下了，可能会先做 s7-s9 拿首冠这一块

---

## 二、这四个纪元分别是什么

| 纪元 | 年份 | 世界冠军 | 这一段的故事内核 |
|---|---|---|---|
| **S3–S6 魔王纪元** | 2013–2016 | SKT / 三星白 / SKT / SKT | Faker 的统治期。LPL 一座世界冠军都没有，「LPL 什么时候能赢」是那几年的空气 |
| **S7–S9 暗夜与双冠** | 2017–2019 | 三星 Galaxy / **IG** / **FPX** | 从 S7 决赛的暗夜（SSG 3:0 SKT）到 S8 IG 首冠、S9 FPX 卫冕。**LPL 从零到二** |
| **S10–S12** | 2020–2022 | DWG / **EDG** / DRX | 疫情、EDG 夺冠、S12 DRX 黑马。S12 就是现在游戏的开局年 |
| **S13–S15** | 2023–2025 | T1 三连 | 和现在这条时间线**重叠** |

**第四个纪元要单独说：**现在的游戏从 2022 开局，2023–2025（S13–S15）是玩家自己在游戏里打出来的**架空线**——
`SEASONS` 里 S13–S15 的版本主题、剧情都是拟的，世界冠军由模拟决定。
所以「S13–S15 一个纪元」不是新增内容，而是**把这三年换成史实开局**（T1 三连是既定强敌）。
它和前三个纪元的性质不一样，值得单独决定要不要做。

真正的新内容是 **S3–S6、S7–S9、S10–S11**。

---

## 三、现在的代码离它有多远

好消息是骨架基本是通的，坏消息是数据要重爬。逐项看：

### 已经参数化、基本能复用

| 部件 | 现状 | 结论 |
|---|---|---|
| 赛制（MSI / 世界赛） | `SEASONS[i].msi={mode,playin}`、`worlds={playin,main}` 每年一套配置，`intl.ts` 按它跑 | **能复用**，只要给新纪元填对当年的赛制 |
| 赛区定锚 | `REGION_ANCHOR` / `REGION_STYLE` / `REGION_SYN` 是一张表 | **能复用**，每个纪元一份 |
| 赛区统治力曲线 | `LCK_DYNASTY` 按赛季下标取值 | **能复用**，但每个纪元要重配（魔王纪元这条曲线会很陡） |
| 明星聚光灯 | `stars.ts` 的 `STARS` 是「真名 → 绰号 / 履历」的字典 | **能复用**，每个纪元换一份名单 |
| 二队 / 青训 | `LDL_ROSTER` 是 2022 的真实 LDL 名单 | 老纪元没有 LDL（LSPL / LDL 建制不同），要按当年制度重做 |
| 存档 | 整个 `S` 序列化，加一个 `S.era` 自动进档 | **能复用** |

### 要重做的

| 部件 | 现状 | 缺口 |
|---|---|---|
| **比赛数据** | `data/csv/game_data_2022.json` 一个文件，`export_game.py` 里 `season` **写死 2022** | 每个纪元要一份 `game_data_<年>.json` |
| **原始数据** | `build_leaguepedia.py` 读 `data/raw/leaguepedia_lol_data.json.gz`，源表是 `rosters_2022plus` / `lspl_historic` | 这份 dump **只覆盖 2022+**，而且 `data/raw/*.gz` 在 `.gitignore` 里、不进仓库 —— **老赛季必须重爬**（作者说的就是这件事） |
| **数据加载** | `demo/src/data.ts` 静态 `import gameData from ".../game_data_2022.json"` | 要改成多份数据 + 按 `S.era` 选，产物体积也会跟着涨 |
| **赛季表** | `SEASONS` 是手写的八条（版本主题、强势位置、关键属性、剧情） | 每个纪元手写 3–4 条，**这部分没法自动生成**，是写作量 |
| **传奇 / 复出池** | `DATA.legends` + `LEGEND_HOME` 按 2022 的时点写的 | 每个纪元的「老将」不是同一批人 |

### 一个容易忽略的坑

**S3–S5 没有 MSI**（MSI 从 2015 才有），S3 也没有入围赛。
`intl.ts` 现在假定每年都有 MSI —— 魔王纪元要支持「这一年没有 MSI」这个形态。
同理，那几年的联赛还叫 LPL / OGN / LCS EU / LCS NA，赛区代号和队名都要跟着改。

---

## 四、分期路线（按作者说的，先做 S7–S9）

**先做 S7–S9 是对的**，理由不只是「作者想做」：

1. **故事最强。**S8 IG 首冠是 LPL 玩家的集体记忆，「拿首冠」这个目标自带情绪，
   而且它天然是一个**有终点的三年**——比开放式的五年更适合做第一个纪元。
2. **数据最近、最好爬。**2017–2019 的 Leaguepedia 数据完整度远好于 2013–2016。
3. **赛制最接近现在。**有 MSI、有入围赛、有小组赛，`intl.ts` 基本不用动结构。
4. **它能验证整套「纪元」框架**——跑通一个，后面三个是重复劳动而不是新设计。

建议的落地顺序：

| 期 | 内容 |
|---|---|
| **第一期** | 框架：`S.era`、多份 `game_data_*.json`、建档页选纪元、存档兼容。**先用 2022 那份数据跑通框架**，不引入新数据 |
| **第二期** | S7–S9 数据落地：爬 2017–2019、过一遍 `fit_ratings` / `blend`、手写三条 `SEASONS`、配 `LCK_DYNASTY` 与 `STARS` |
| **第三期** | S7–S9 的手感标定：批测「LPL 首冠」的难度，让 S8 夺冠既不是必然也不是奢望 |
| **以后** | S10–S12 → S3–S6（最难，赛制差异最大）→ S13–S15（要先决定它和现有架空线怎么共存） |

**第一期和第二期拆开是关键**：框架不该等数据。先让「选纪元」这件事在只有一个纪元的情况下也能跑通，
后面每加一个纪元就只是加数据 + 写三条赛季表。

---

## 五、每个纪元需要准备的东西（清单）

做第二期时照这张单子走：

1. `game_data_<起始年>.json`：各赛区队伍、名单、五维、真实赛季胜率
2. `SEASONS` 三到四条：年份、代号、版本主题、强势位置、关键属性、剧情一句话、MSI / 世界赛赛制
3. `REGION_ANCHOR` 一份：那几年各赛区的相对水位（S3–S6 的 LCK 要明显更高）
4. `LCK_DYNASTY`（或改名成更中性的「赛区统治力」）一份：逐年
5. `STARS` 一份：那个时代的明星 + 履历（履历要写到开局那年为止）
6. 次级联赛名单一份（LSPL / LDL，按当年建制）
7. 传奇 / 复出池一份
8. 冠军史实：哪一年谁夺冠，用于「你改写了历史」的对照叙事

---

## 六、最难的三件事（先说清楚，别到时候才发现）

1. **数值校对，不是爬数据。**爬是体力活，难的是**让不同年代的数据落在同一把尺上**。
   现在全游戏只有一把实力尺（五维均值，`REGION_ANCHOR` 定锚）。2013 年的选手数据口径和 2019 年不一样，
   直接喂进去会出现「S3 的 Faker 只有 70 分」这类荒谬。每个纪元都要重跑一次 `fit_ratings` 并人工校一遍头部选手。
2. **产物体积。**`career.html` 是单文件、全内联，现在 1.28 MB。每加一个纪元的数据就多一份 JSON，
   四个纪元可能把首屏体积推到 3 MB 以上。要么按需加载（和「单文件」这个发布形态冲突），
   要么每个纪元出一个独立的 HTML。**这个取舍要在第一期就定下来**，不然第三个纪元时会推倒重来。
3. **明星履历的时间点。**`STARS` 里每个人的履历是「写到开局前一年为止」，之后由玩家改写。
   四个纪元就是四份履历表，而且同一个人（Faker）在四个纪元里的履历完全不同。这是纯写作量，没有捷径。

---

## 七、明确不做的

- **不做「一条时间线从 S3 打到 S15」。**那是十三年生涯，年龄曲线和现有的五年结构完全冲突。
  纪元是**四个独立的开局**，不是一条长线。
- **不做跨纪元存档迁移。**选了纪元就是选了一个世界，中途不能换。
- **不在第一期碰 S13–S15。**它和现有架空线重叠，要先想清楚「史实开局」和「玩家已经打出来的 2023–2025」怎么并存。

---

## 七点五、2026-09-08 作者拍板与首个纪元落地

作者定了三条，据此做了 S6–S11 的 demo（见 PR、`demo/src/eras.ts`）：

1. **不同纪元的数据不共通** —— 各带各的名单快照
2. **每个纪元用自己的标尺** —— 各带各的 `REGION_ANCHOR`，不做跨年代校准
3. **先写代码框架，数据后期填进去**

第 2 条把本稿第六节里标为「最难」的那件事（跨年代数值校对）**直接消掉了**：
`anchorLeague` 本来就把每个联赛的均值钉到该赛区锚点，所以手写数据只要
**联赛内部的相对强弱**对，绝对值由纪元自己的锚点决定。

第 3 条对应的落地：框架做完整、数据留成可替换的槽。已经就位的是——
纪元注册表（`eras.ts`）、活绑定切换（不改那 96 处 `SEASONS[...]` 的读取点）、
`S.era` 进存档、建档页选纪元、**纪元自检**（`npm test` 会把每个注册过的纪元
逐个校验数据形状并整局跑一遍）。加纪元的四步操作手册写在 `eras.ts` 的文件头。

第六节里担心的**产物体积**也证伪了：加一个纪元只涨 4.8%（+57 KB），
四个纪元约 +240 KB，单文件形态保得住。

### 以后要做的（作者已定方向，不在当前范围）

- **把纪元连起来**：打完 S6–S11 接着进 S12–S16，同一个角色跨纪元。
  挡路的是 `SEASONS` 整表替换、`S.era` 建档时定死、生涯长度按纪元算——
  要把「赛季表」从纪元的属性改成一条可拼接的时间轴，年龄曲线和退役线按总年数重算。
  不是小改动，但现在这套结构没有堵死它。
- **统一标尺**：各纪元的 anchor 合并成一张跨年代可比的表。
  `anchorLeague` 的调用方式不用变，改的只是 anchor 的来源——比「连起来」容易。

---

## 八、备注

- 这一稿只记录思路。真正动工前要重新评估：那时候的代码、数据管线、发布形态可能都变了。
- 作者原话「一个个做可能上线的比较慢」是对的——建议**每个纪元单独上线**，
  不要攒齐四个再发。第一个纪元（S7–S9）上线后看玩家反馈，再决定要不要做完剩下三个。
